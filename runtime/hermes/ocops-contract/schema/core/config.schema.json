{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ocops Config",
  "description": "实例当前运行配置快照，供 manager 实时查询，不依赖 DB 快照。",
  "type": "object",
  "required": ["display_language"],
  "properties": {
    "display_language": {
      "type": "string",
      "description": "实例当前使用的界面语言代码，来自 /opt/data/config.yaml 的 display.language 字段；缺失时回落 en。"
    }
  },
  "additionalProperties": false
}
