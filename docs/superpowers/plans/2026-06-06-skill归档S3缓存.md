# 第三方市场 skill 归档 S3 缓存 + 上游失败错误处理 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 clawhub（及未来第三方市场）的 skill 归档下载加一层「先读 S3 缓存、未命中再回源并写回」的通用 read-through 缓存，并把上游下载失败从泛化 500 改为明确的 502。

**Architecture:** 新增 source 无关的 `SkillArchiveCache`（按 `(source, ref, version, ext)` 寻址、复用现有 `LibraryBlobStore` 与 `library/` 布局、按版本永久缓存），注入到三个回源点（市场下载 `ClawHubSource.Download`、实例安装/更新 `AppSkillService`、助手版本加 skill `AssistantVersionService`）；上游非 2xx / 网络错误统一包成新哨兵 `ErrSkillMarketUpstreamUnavailable`，两个 handler 映射为 502。`platform` 来源本就读本地持久副本、不回源，保持不变。

**Tech Stack:** Go 1.22+、testify（assert/require）、gin、现有 `internal/integrations/storage`（S3 / FS 抽象）、Vitest（前端仅验证不改码）。

**前置规范：** 遵守仓库 AGENTS.md——改业务逻辑必补/改单测（每个测试方法与 table 用例带相邻中文注释）；新增代码补中文注释；提交用 Conventional Commits（中文摘要 + 正文）；交付前跑相关测试。

---

## 背景与诊断（已取证）

- 浏览器复现：`GET /api/v1/skill-market/download?source=clawhub&ref=self-improving&version=1.2.16` → 500「服务器内部错误」；点「安装」`POST /api/v1/apps/<id>/skills` 同样 500。
- 上游探测：`GET https://clawhubcn.com/api/v1/download?slug=self-improving&version=1.2.16` 返回 **502 "Download failed"**（clawhub 自身网关错误）；对照 skill-vetter / weather / github 等均 200 + 合法 zip。
- 根因：`clawhub.Download` 把上游 502 包成普通 `error` → `writeSkillMarketError` / `writeAppSkillError` 的 default 分支 → 泛化 500。
- 设计文档：`docs/superpowers/specs/2026-06-06-skill归档S3缓存-design.md`。
- 决策：① 按版本永久缓存（无 TTL）；② 上游坏掉返回明确错误即可（不做置灰/预探测）。
- 注意：缓存**不能**修复 self-improving（从未成功拉取过 → 缓存空 → 首次仍撞上游 502），它的唯一改进是错误显示清楚。

## File Structure

| 文件 | 责任 | 动作 |
|---|---|---|
| `internal/service/skill_archive_cache.go` | 通用 read-through 归档缓存 `SkillArchiveCache` | **新建** |
| `internal/service/skill_archive_cache_test.go` | 上面的单测 | **新建** |
| `internal/service/errors.go` | 新增哨兵 `ErrSkillMarketUpstreamUnavailable` | 改 |
| `internal/api/handlers/skill_market.go` | `writeSkillMarketError` 加 502 分支 | 改 |
| `internal/api/handlers/app_skills.go` | `writeAppSkillError` 加 502 分支 | 改 |
| `internal/api/handlers/skill_market_test.go` | 市场下载 502 映射测试 | 改 |
| `internal/api/handlers/app_skills_test.go` | 安装 502 映射测试 | 改 |
| `internal/service/clawhub_source.go` | `ClawHubSource` 注入 cache、`Download` 走 read-through + 包装上游错误 | 改 |
| `internal/service/clawhub_source_test.go` | 更新构造调用 + 新增下载缓存/上游失败测试 | 改 |
| `internal/service/app_skill_service.go` | 加 `cache` 字段；`fetchArchive` clawhub 走 read-through、校验内移、返回 relPath；`Install`/`Update` 用 relPath、去掉显式 Put | 改 |
| `internal/service/app_skill_service_test.go` | 新增 clawhub 缓存命中 / 上游失败测试 | 改 |
| `internal/service/assistant_version_service.go` | `resolveLibrarySkill` clawhub 走 read-through + 包装上游错误 | 改 |
| `internal/service/assistant_version_service_test.go` | 新增 clawhub 上游失败测试 | 改 |
| `cmd/server/main.go` | 构造 `archiveCache` 并注入 `NewClawHubSource` | 改 |

无 DB 迁移、无 OpenAPI 契约变更、前端零改码。

---

## Task 1: `SkillArchiveCache` 通用 read-through 缓存

**Files:**
- Create: `internal/service/skill_archive_cache.go`
- Test: `internal/service/skill_archive_cache_test.go`

说明：缓存键复用 `storage.LibrarySkillKey(source, ref, version, ext)` = `library/<source>/<ref>/<version>.<ext>`。读缓存任何失败（对象不存在 / 抖动）都降级为未命中、回源——缓存是优化而非硬依赖。测试复用 `internal/service/platform_skill_service_test.go` 里已有的 `fakeLibraryBlob`（同包，直接可用，其 `OpenLibrarySkill` 未命中时返回普通 error，正好验证「任意读失败都当未命中」）。

- [ ] **Step 1: 写失败测试**

创建 `internal/service/skill_archive_cache_test.go`：

```go
package service

import (
	"context"
	"errors"
	"testing"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)

// archiveCacheCounter 包一个回源计数器，便于断言「命中时不回源」。
type archiveCacheCounter struct {
	calls int      // fetch 闭包被调用次数
	data  []byte   // 回源返回的字节
	err   error    // 回源返回的错误（非 nil 时模拟上游失败）
}

func (c *archiveCacheCounter) fetch(_ context.Context) ([]byte, error) {
	c.calls++
	if c.err != nil {
		return nil, c.err
	}
	return c.data, nil
}

// TestSkillArchiveCache_Miss_FetchesAndWrites 未命中：回源一次、写回缓存、返回字节与确定性相对路径。
func TestSkillArchiveCache_Miss_FetchesAndWrites(t *testing.T) {
	blobs := &fakeLibraryBlob{}
	cache := NewSkillArchiveCache(blobs)
	up := &archiveCacheCounter{data: []byte("ZIP")}

	data, rel, err := cache.Fetch(context.Background(), "clawhub", "weather", "1.0", "zip", up.fetch)
	require.NoError(t, err)
	assert.Equal(t, []byte("ZIP"), data)
	// 相对路径为 library/<source>/<ref>/<version>.<ext>
	assert.Equal(t, "library/clawhub/weather/1.0.zip", rel)
	// 未命中应回源一次
	assert.Equal(t, 1, up.calls)
	// 已写回缓存（fakeLibraryBlob.stored 收到对应 key）
	assert.Equal(t, []byte("ZIP"), blobs.stored["library/clawhub/weather/1.0.zip"])
}

// TestSkillArchiveCache_Hit_SkipsUpstream 命中：直接返回缓存字节，绝不回源。
func TestSkillArchiveCache_Hit_SkipsUpstream(t *testing.T) {
	blobs := &fakeLibraryBlob{stored: map[string][]byte{
		"library/clawhub/weather/1.0.zip": []byte("CACHED"),
	}}
	cache := NewSkillArchiveCache(blobs)
	// 回源闭包预置错误：一旦被调用即说明没命中缓存，测试应失败。
	up := &archiveCacheCounter{err: errors.New("上游不该被调用")}

	data, rel, err := cache.Fetch(context.Background(), "clawhub", "weather", "1.0", "zip", up.fetch)
	require.NoError(t, err)
	assert.Equal(t, []byte("CACHED"), data)
	assert.Equal(t, "library/clawhub/weather/1.0.zip", rel)
	// 命中缓存 → 回源 0 次
	assert.Equal(t, 0, up.calls)
}

// TestSkillArchiveCache_UpstreamError_NoWrite 未命中且回源失败：原样返回错误、不写缓存。
func TestSkillArchiveCache_UpstreamError_NoWrite(t *testing.T) {
	blobs := &fakeLibraryBlob{}
	cache := NewSkillArchiveCache(blobs)
	sentinel := errors.New("上游 502")
	up := &archiveCacheCounter{err: sentinel}

	_, _, err := cache.Fetch(context.Background(), "clawhub", "x", "1.0", "zip", up.fetch)
	require.ErrorIs(t, err, sentinel)
	// 回源失败不写缓存
	assert.Nil(t, blobs.stored["library/clawhub/x/1.0.zip"])
}

// nilReturningBlob 是一个「无对象时返回 (nil, nil)」的 LibraryBlobStore 替身，
// 用于验证 Fetch 对此类实现的 rc!=nil 守卫——不 panic、按未命中回源。
type nilReturningBlob struct{ put map[string][]byte }

func (b *nilReturningBlob) PutLibrarySkill(source, ref, version, ext string, data []byte) (string, error) {
	if b.put == nil {
		b.put = map[string][]byte{}
	}
	rel := "library/" + source + "/" + ref + "/" + version + "." + ext
	b.put[rel] = data
	return rel, nil
}
func (b *nilReturningBlob) DeleteLibrarySkill(string) error { return nil }
func (b *nilReturningBlob) OpenLibrarySkill(string) (io.ReadCloser, error) { return nil, nil } // 故意返回 (nil, nil)

// TestSkillArchiveCache_NilReadCloser_NoPanic OpenLibrarySkill 返回 (nil, nil) 时不 panic，按未命中回源并写回。
func TestSkillArchiveCache_NilReadCloser_NoPanic(t *testing.T) {
	blobs := &nilReturningBlob{}
	cache := NewSkillArchiveCache(blobs)
	up := &archiveCacheCounter{data: []byte("ZIP")}

	data, rel, err := cache.Fetch(context.Background(), "clawhub", "weather", "1.0", "zip", up.fetch)
	require.NoError(t, err)
	assert.Equal(t, []byte("ZIP"), data)
	assert.Equal(t, "library/clawhub/weather/1.0.zip", rel)
	// (nil, nil) 被当作未命中 → 回源一次并写回。
	assert.Equal(t, 1, up.calls)
	assert.Equal(t, []byte("ZIP"), blobs.put["library/clawhub/weather/1.0.zip"])
}
```

> 注：测试文件 import 块需包含 `"io"`（`nilReturningBlob` 用到 `io.ReadCloser`）。

- [ ] **Step 2: 运行测试确认失败**

Run: `go test ./internal/service/ -run TestSkillArchiveCache -v`
Expected: 编译失败（`undefined: NewSkillArchiveCache`）。

- [ ] **Step 3: 实现 `SkillArchiveCache`**

创建 `internal/service/skill_archive_cache.go`：

```go
// Package service：第三方市场 skill 归档的通用读穿（read-through）缓存。
package service

import (
	"context"
	"io"

	"oc-manager/internal/integrations/storage"
)

// SkillArchiveCache 是 skill 归档的读穿缓存，底层走 LibraryBlobStore（FS 本地 / S3 生产）。
//
// 设计意图：让 clawhub 及未来任意第三方市场的归档下载「成功一次即缓存、后续免回源」，
// 并扛住上游偶发抖动。缓存按 (source, ref, version) 抽象寻址，对具体来源无感知——
// 新增市场只需在自己的回源点传入一个下载闭包即可复用本缓存。
//
// 约束：
//   - 按版本永久缓存（无 TTL）：归档按版本号本应不可变，命中即长期有效。
//   - 缓存是性能优化而非硬依赖：读缓存出错（对象不存在 / 对象存储抖动）一律降级为未命中、
//     回源，绝不因缓存问题让请求失败。
type SkillArchiveCache struct {
	// blobs 是归档对象存储，读写共享 library/ 前缀。
	blobs LibraryBlobStore
}

// NewSkillArchiveCache 构造读穿缓存，包装一个 LibraryBlobStore。
func NewSkillArchiveCache(blobs LibraryBlobStore) *SkillArchiveCache {
	return &SkillArchiveCache{blobs: blobs}
}

// Fetch 取 (source, ref, version) 归档的字节与其在对象存储中的相对路径：
//  1. 先读缓存 OpenLibrarySkill(LibrarySkillKey(...))；命中即返回缓存字节（不回源）。
//  2. 读缓存出错或对象不存在 → 视为未命中，调 fetch 回源。
//  3. fetch 成功 → PutLibrarySkill 写回 → 返回 (data, relPath, nil)。
//  4. fetch 失败 → 原样返回该错误（由调用方分类映射为 502 等），不写缓存。
//
// ext 决定缓存 key 后缀（clawhub=zip，platform=tar）。回源下载与（按需）解压安全校验由
// fetch 闭包负责：校验失败时 fetch 返回错误 → 不会写入缓存，避免缓存到不安全归档。
func (c *SkillArchiveCache) Fetch(
	ctx context.Context,
	source, ref, version, ext string,
	fetch func(ctx context.Context) ([]byte, error),
) (data []byte, relPath string, err error) {
	key := storage.LibrarySkillKey(source, ref, version, ext)
	// 1) 读缓存：命中即返回。任何读失败（不存在 / 抖动）都降级为未命中，继续回源。
	//    额外守卫 rc != nil：个别 LibraryBlobStore 实现可能在「无对象」时返回 (nil, nil)，
	//    若不防护会在 io.ReadAll(nil) 处 panic；此处一并按未命中处理。
	if rc, oerr := c.blobs.OpenLibrarySkill(key); oerr == nil && rc != nil {
		cached, rerr := io.ReadAll(rc)
		_ = rc.Close()
		if rerr == nil {
			return cached, key, nil
		}
		// 读到一半失败：降级回源，不因缓存读异常中断请求。
	}
	// 2) 未命中：回源。失败原样上抛、不写缓存。
	fetched, ferr := fetch(ctx)
	if ferr != nil {
		return nil, "", ferr
	}
	// 3) 写回缓存。写失败不阻断本次请求（归档已在手）：返回确定性 key，
	//    与 Put 成功时一致——极端情况下缓存暂时缺对象，下次回源会再写。
	if written, werr := c.blobs.PutLibrarySkill(source, ref, version, ext, fetched); werr == nil {
		return fetched, written, nil
	}
	return fetched, key, nil
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `go test ./internal/service/ -run TestSkillArchiveCache -v`
Expected: PASS（3 个用例全绿）。

- [ ] **Step 5: 提交**

```bash
git add internal/service/skill_archive_cache.go internal/service/skill_archive_cache_test.go
git commit -m "feat(skill): 增加第三方市场归档通用 read-through 缓存

新增 SkillArchiveCache：按 (source, ref, version, ext) 寻址、复用现有
LibraryBlobStore 与 library/ 布局，命中即免回源、未命中回源后写回。
按版本永久缓存（无 TTL），读缓存出错降级为未命中、不让缓存成为硬依赖。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2: 上游失败哨兵 + 两个 handler 的 502 映射

**Files:**
- Modify: `internal/service/errors.go`（在 `ErrSkillMarketInvalid` 定义之后追加）
- Modify: `internal/api/handlers/skill_market.go:148-163`（`writeSkillMarketError`）
- Modify: `internal/api/handlers/app_skills.go:192-219`（`writeAppSkillError`）
- Test: `internal/api/handlers/skill_market_test.go`、`internal/api/handlers/app_skills_test.go`

- [ ] **Step 1: 写失败测试（两个 handler 各一个）**

在 `internal/api/handlers/skill_market_test.go` 末尾追加：

```go
// TestSkillMarketHandler_Download_Upstream502 验证 service 返回 ErrSkillMarketUpstreamUnavailable
// 时 handler 响应 502 且文案明确（区别于泛化 500「服务器内部错误」）。
func TestSkillMarketHandler_Download_Upstream502(t *testing.T) {
	// 上游下载失败场景：stub 的 Download 返回上游不可用哨兵。
	stub := &skillMarketServiceStub{downloadErr: service.ErrSkillMarketUpstreamUnavailable}
	gin.SetMode(gin.TestMode)
	r := gin.New()
	RegisterSkillMarketRoutes(r, NewSkillMarketHandler(stub))

	req := httptest.NewRequest(http.MethodGet, "/api/v1/skill-market/download?source=clawhub&ref=x&version=1.0", nil)
	rec := httptest.NewRecorder()
	r.ServeHTTP(rec, withPrincipal(req, auth.Principal{UserID: "u1"}))

	// 上游故障映射为 502 Bad Gateway，文案明确。
	assert.Equal(t, http.StatusBadGateway, rec.Code)
	assert.Contains(t, rec.Body.String(), "上游技能市场暂时不可用")
}
```

在 `internal/api/handlers/app_skills_test.go` 末尾追加：

```go
// TestAppSkillsHandler_Install_UpstreamUnavailable 验证安装时上游下载失败映射为 502。
func TestAppSkillsHandler_Install_UpstreamUnavailable(t *testing.T) {
	// 上游故障场景：service 返回 ErrSkillMarketUpstreamUnavailable。
	stub := &appSkillServiceStub{installErr: service.ErrSkillMarketUpstreamUnavailable}
	router := newAppSkillsTestRouter(t, stub)

	body, _ := json.Marshal(map[string]string{
		"source": "clawhub", "source_ref": "self-improving", "name": "self-improving", "version": "1.2.16",
	})
	w := httptest.NewRecorder()
	req := httptest.NewRequest(http.MethodPost, "/api/v1/apps/app-1/skills", bytes.NewReader(body))
	req.Header.Set("Content-Type", "application/json")
	req = withPrincipal(req, auth.Principal{UserID: "u1", Role: domain.UserRoleOrgAdmin, OrgID: "org-1"})
	router.ServeHTTP(w, req)

	// 上游故障映射为 502，文案明确。
	assert.Equal(t, http.StatusBadGateway, w.Code)
	assert.Contains(t, w.Body.String(), "上游技能市场暂时不可用")
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `go test ./internal/api/handlers/ -run 'TestSkillMarketHandler_Download_Upstream502|TestAppSkillsHandler_Install_UpstreamUnavailable' -v`
Expected: 编译失败（`undefined: service.ErrSkillMarketUpstreamUnavailable`）。

- [ ] **Step 3: 加哨兵**

在 `internal/service/errors.go` 的 `ErrSkillMarketInvalid` 定义之后追加：

```go
// ErrSkillMarketUpstreamUnavailable 表示上游第三方市场归档下载失败（非 2xx / 网络错误），
// 且本地缓存未命中、无法降级。映射为 502 Bad Gateway，与「manager 自身 500」语义区分开——
// 让前端能据此提示用户「上游暂时不可用、稍后重试」，而非误以为 manager 故障。
var ErrSkillMarketUpstreamUnavailable = errors.New("上游技能市场暂时不可用")
```

- [ ] **Step 4: 加两个 502 分支**

在 `internal/api/handlers/skill_market.go` 的 `writeSkillMarketError` 中，`ErrSkillMarketDenied` 分支之后、`default` 之前插入：

```go
	case errors.Is(err, service.ErrSkillMarketUpstreamUnavailable):
		// 上游市场下载失败（如 clawhub 返 502）且缓存未命中：502 Bad Gateway，区别于 manager 自身 500。
		c.JSON(http.StatusBadGateway, apierror.New("UPSTREAM_UNAVAILABLE", "上游技能市场暂时不可用，请稍后重试"))
```

在 `internal/api/handlers/app_skills.go` 的 `writeAppSkillError` 中，`ErrAppSkillRuntimeUnsupported` 分支之后、`default` 之前插入：

```go
	// 上游市场下载失败（缓存未命中无法降级）→ 502，区别于 manager 自身 500
	case errors.Is(err, service.ErrSkillMarketUpstreamUnavailable):
		c.JSON(http.StatusBadGateway, apierror.New("UPSTREAM_UNAVAILABLE", "上游技能市场暂时不可用，请稍后重试"))
```

- [ ] **Step 5: 运行测试确认通过**

Run: `go test ./internal/api/handlers/ -run 'TestSkillMarketHandler_Download_Upstream502|TestAppSkillsHandler_Install_UpstreamUnavailable' -v`
Expected: PASS。

- [ ] **Step 6: 提交**

```bash
git add internal/service/errors.go internal/api/handlers/skill_market.go internal/api/handlers/app_skills.go internal/api/handlers/skill_market_test.go internal/api/handlers/app_skills_test.go
git commit -m "feat(skill): 上游市场下载失败返回明确 502 而非泛化 500

新增 ErrSkillMarketUpstreamUnavailable 哨兵，下载与安装两个 handler
各加一个 502 Bad Gateway 分支，文案「上游技能市场暂时不可用，请稍后重试」，
不再把 clawhub 等上游故障误报成 manager 自身的「服务器内部错误」。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: `ClawHubSource.Download` 走 read-through + 包装上游错误

**Files:**
- Modify: `internal/service/clawhub_source.go`（struct + `NewClawHubSource` + `Download`，加 `fmt` import）
- Modify: `internal/service/clawhub_source_test.go`（3 处构造调用 + 下载测试）
- Modify: `cmd/server/main.go:313-326, 504`（构造 `archiveCache` 并注入）

- [ ] **Step 1: 改下载缓存测试**

替换 `internal/service/clawhub_source_test.go` 中现有的 `TestClawHubSource_Download`（约 169-179 行）为下面三个测试；其中 `&fakeLibraryBlob{}` 与 `NewSkillArchiveCache` 均为同包已有类型：

```go
// TestClawHubSource_Download_MissWritesCache 未命中：回源下载、写回缓存、返回 zip 字节。
func TestClawHubSource_Download_MissWritesCache(t *testing.T) {
	api := &fakeClawHubAPI{archive: []byte("ZIP-ARCHIVE-BYTES")}
	blobs := &fakeLibraryBlob{}
	src := NewClawHubSource(api, newFakeRedis(), time.Minute, NewSkillArchiveCache(blobs))

	got, ext, err := src.Download(context.Background(), "self-improving-agent", "3.0.21")
	require.NoError(t, err)
	assert.Equal(t, []byte("ZIP-ARCHIVE-BYTES"), got)
	// ClawHub 归档格式固定 zip。
	assert.Equal(t, "zip", ext)
	// 已写回缓存：key = library/clawhub/<slug>/<version>.zip。
	assert.Equal(t, []byte("ZIP-ARCHIVE-BYTES"), blobs.stored["library/clawhub/self-improving-agent/3.0.21.zip"])
}

// TestClawHubSource_Download_HitSkipsUpstream 命中缓存：直接返回缓存字节，不调上游 API。
func TestClawHubSource_Download_HitSkipsUpstream(t *testing.T) {
	// 上游归档为 nil：若被调用会返回空字节，测试据此证明走的是缓存而非上游。
	api := &fakeClawHubAPI{archive: nil}
	blobs := &fakeLibraryBlob{stored: map[string][]byte{
		"library/clawhub/weather/1.0.zip": []byte("CACHED-ZIP"),
	}}
	src := NewClawHubSource(api, newFakeRedis(), time.Minute, NewSkillArchiveCache(blobs))

	got, _, err := src.Download(context.Background(), "weather", "1.0")
	require.NoError(t, err)
	// 命中缓存返回的是缓存字节。
	assert.Equal(t, []byte("CACHED-ZIP"), got)
}

// TestClawHubSource_Download_UpstreamErrorWrapped 上游下载报错时包成 ErrSkillMarketUpstreamUnavailable。
func TestClawHubSource_Download_UpstreamErrorWrapped(t *testing.T) {
	// fakeClawHubAPI.Download 永远成功，故另用一个会报错的 API 替身。
	api := &erroringClawHubAPI{}
	src := NewClawHubSource(api, newFakeRedis(), time.Minute, NewSkillArchiveCache(&fakeLibraryBlob{}))

	_, _, err := src.Download(context.Background(), "x", "1.0")
	// 上游故障须可被 errors.Is 识别为上游不可用，供 handler 映射 502。
	require.ErrorIs(t, err, ErrSkillMarketUpstreamUnavailable)
}

// erroringClawHubAPI 是 Download 永远报错的 ClawHubSearcher 替身（仅本测试用）。
type erroringClawHubAPI struct{ fakeClawHubAPI }

func (e *erroringClawHubAPI) Download(_ context.Context, _, _ string) ([]byte, error) {
	return nil, errors.New("clawhub 下载返回非 2xx: 502")
}
```

并把 `clawhub_source_test.go` 现有 import 块补上 `"errors"`（若尚未导入）。同时把另外两处 Search 测试的构造调用（约 113、148 行）从
`NewClawHubSource(api, rdb, time.Minute)` 改为
`NewClawHubSource(api, rdb, time.Minute, NewSkillArchiveCache(&fakeLibraryBlob{}))`。

- [ ] **Step 2: 运行测试确认失败**

Run: `go test ./internal/service/ -run TestClawHubSource -v`
Expected: 编译失败（`NewClawHubSource` 参数个数不符）。

- [ ] **Step 3: 改 `ClawHubSource`**

在 `internal/service/clawhub_source.go`：导入块加 `"fmt"`；struct 加 `cache` 字段；改构造函数；重写 `Download`。

struct（在 `ttl time.Duration` 字段后加一行）：

```go
	// cache 是归档读穿缓存：clawhub 下载先查 library/clawhub/<slug>/<ver>.zip，命中即免回源。
	cache *SkillArchiveCache
```

构造函数：

```go
// NewClawHubSource 构造公共库来源。cache 为归档读穿缓存（市场下载/安装共用同一份缓存）。
func NewClawHubSource(api ClawHubSearcher, rdb RedisCache, ttl time.Duration, cache *SkillArchiveCache) *ClawHubSource {
	return &ClawHubSource{api: api, rdb: rdb, ttl: ttl, cache: cache}
}
```

`Download` 整体替换为：

```go
// Download 取公共库 slug=ref、version 的归档原始字节，扩展名固定 zip。
// 走读穿缓存：命中 library/clawhub/<slug>/<ver>.zip 即免回源；未命中回源 ClawHub /download 并写回。
// 上游非 2xx / 网络错误包成 ErrSkillMarketUpstreamUnavailable，供聚合层/ handler 映射为 502。
func (s *ClawHubSource) Download(ctx context.Context, ref, version string) ([]byte, string, error) {
	data, _, err := s.cache.Fetch(ctx, "clawhub", ref, version, "zip", func(fctx context.Context) ([]byte, error) {
		b, derr := s.api.Download(fctx, ref, version)
		if derr != nil {
			return nil, fmt.Errorf("%w: %v", ErrSkillMarketUpstreamUnavailable, derr)
		}
		return b, nil
	})
	if err != nil {
		return nil, "", err
	}
	return data, "zip", nil
}
```

- [ ] **Step 4: 改 wiring 让其编译**

在 `cmd/server/main.go`：在 `libraryBlobs` 完成赋值之后（约 326 行 `platformSkillService := ...` 之前）插入：

```go
	// archiveCache 是第三方市场归档读穿缓存，市场下载与（间接）安装/版本共用：复用 libraryBlobs 同一对象存储。
	archiveCache := service.NewSkillArchiveCache(libraryBlobs)
```

把约 504 行的
`clawhubSource = service.NewClawHubSource(clawhubClient, imagecoordRedis, cfg.ClawHub.CacheTTL.Duration)`
改为：
`clawhubSource = service.NewClawHubSource(clawhubClient, imagecoordRedis, cfg.ClawHub.CacheTTL.Duration, archiveCache)`

- [ ] **Step 5: 运行测试 + 构建确认通过**

Run: `go test ./internal/service/ -run TestClawHubSource -v && go build ./...`
Expected: 测试 PASS、构建成功。

- [ ] **Step 6: 提交**

```bash
git add internal/service/clawhub_source.go internal/service/clawhub_source_test.go cmd/server/main.go
git commit -m "feat(skill): 市场下载 clawhub 归档走 S3 读穿缓存

ClawHubSource.Download 改为经 SkillArchiveCache：命中
library/clawhub/<slug>/<ver>.zip 即免回源，未命中回源后写回；上游非 2xx
包成 ErrSkillMarketUpstreamUnavailable。main.go 构造共享 archiveCache 注入。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: `AppSkillService` 安装/更新走 read-through

**Files:**
- Modify: `internal/service/app_skill_service.go`（struct 加 `cache`；`NewAppSkillService` 初始化；重写 `fetchArchive`；`Install`/`Update` 用 relPath、去掉显式 `PutLibrarySkill` 与 `validateArchiveSafety`；加 `storage` import）
- Test: `internal/service/app_skill_service_test.go`（新增 clawhub 缓存命中 / 上游失败测试）

说明：把解压安全校验从 `Install`/`Update` 内**下移进 `fetchArchive`**（platform 分支与 clawhub 回源闭包内各校验一次），并让 `fetchArchive` 直接返回缓存相对路径 `relPath`，从而去掉调用方的显式 `PutLibrarySkill`（写入改由缓存的 read-through 完成），保持单次写入。clawhub 命中缓存时回源闭包不执行，信任首次写入时已校验过的字节。

- [ ] **Step 1: 写失败测试（clawhub 缓存命中 + 上游失败）**

在 `internal/service/app_skill_service_test.go` 的 Install 测试区追加：

```go
// TestAppSkillService_Install_ClawHub_CacheHit clawhub 安装命中缓存：不回源下载即落库 active。
// 预置 clawhub 下载器为「一调用即报错」，命中缓存时不应触发它 → 证明走的是缓存。
func TestAppSkillService_Install_ClawHub_CacheHit(t *testing.T) {
	deps := newAppSkillTestDeps(t)
	// 缓存里已有该 skill 归档（首次安装时写入的效果）。
	deps.blobs.stored = map[string][]byte{"library/clawhub/skill-vetter/1.0.zip": []byte("CACHED-ZIP")}
	// 上游下载器预置错误：命中缓存时不应被调用。
	deps.clawhub.err = errors.New("上游不该被调用")
	svc := deps.service()

	res, err := svc.Install(context.Background(), deps.ownerPrincipal(), "app-1", InstallSkillInput{
		Source: "clawhub", SourceRef: "skill-vetter", Name: "skill-vetter", Version: "1.0",
	})
	require.NoError(t, err)
	// 命中缓存即可落库并热装成功。
	assert.Equal(t, "active", res.Status)
	row, ok := deps.appSkills.get("app-1", "skill-vetter")
	require.True(t, ok)
	// CachedTarPath 指向缓存键。
	assert.Equal(t, "library/clawhub/skill-vetter/1.0.zip", row.CachedTarPath)
}

// TestAppSkillService_Install_ClawHub_UpstreamFail clawhub 安装未命中且上游下载失败：
// 返回 ErrSkillMarketUpstreamUnavailable（供 handler 映射 502），不落库。
func TestAppSkillService_Install_ClawHub_UpstreamFail(t *testing.T) {
	deps := newAppSkillTestDeps(t)
	// 缓存为空（未命中），上游下载器报错。
	deps.clawhub.err = errors.New("clawhub 下载返回非 2xx: 502")
	svc := deps.service()

	_, err := svc.Install(context.Background(), deps.ownerPrincipal(), "app-1", InstallSkillInput{
		Source: "clawhub", SourceRef: "self-improving", Name: "self-improving", Version: "1.2.16",
	})
	require.ErrorIs(t, err, ErrSkillMarketUpstreamUnavailable)
	// 上游失败不落库。
	_, ok := deps.appSkills.get("app-1", "self-improving")
	assert.False(t, ok)
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `go test ./internal/service/ -run 'TestAppSkillService_Install_ClawHub' -v`
Expected: FAIL——`CacheHit` 因当前 `fetchArchive` 仍直连上游（`deps.clawhub.err` 触发）而报错；`UpstreamFail` 错误类型不是 `ErrSkillMarketUpstreamUnavailable`。

- [ ] **Step 3: 改 `AppSkillService`**

(a) `internal/service/app_skill_service.go` import 块加 `"oc-manager/internal/integrations/storage"`。

(b) struct 在 `blobs LibraryBlobStore` 字段后加：

```go
	// cache 是归档读穿缓存（包装同一个 blobs）：clawhub 安装/更新命中即免回源。
	cache *SkillArchiveCache
```

(c) `NewAppSkillService` 的返回体加一行 `cache: NewSkillArchiveCache(deps.Blobs),`：

```go
	return &AppSkillService{
		store:    deps.Store,
		apps:     deps.Apps,
		versions: deps.Versions,
		platform: deps.Platform,
		clawhub:  deps.ClawHub,
		blobs:    deps.Blobs,
		cache:    NewSkillArchiveCache(deps.Blobs),
		ocops:    deps.OcOps,
		audit:    deps.Audit,
	}
```

(d) 重写 `fetchArchive`（签名去掉 `ext`、改为返回 `relPath`，并把解压安全校验内移）：

```go
// fetchArchive 按来源取归档字节、sha256（可能为空）、原始元数据快照、对象存储相对路径。
// 返回的 sha 为空时调用方须自行计算（clawhub 来源不提供 sha）。
//   - platform：读平台库已持久化的归档（library/platform/<name>/<ver>.tar），就地校验安全。
//   - clawhub：经读穿缓存取归档——命中即免回源；未命中回源下载、闭包内校验安全后写回。
//     上游失败包成 ErrSkillMarketUpstreamUnavailable，不安全归档返回 ErrAppSkillArchiveTooDangerous 且不缓存。
func (s *AppSkillService) fetchArchive(ctx context.Context, in InstallSkillInput) (data []byte, sha string, meta map[string]any, relPath string, err error) {
	switch in.Source {
	case "platform":
		// 平台库来源：GetForInstall 返回归档字节与预存 sha256
		d, sh, e := s.platform.GetForInstall(ctx, in.SourceRef, in.Version)
		if e != nil {
			return nil, "", nil, "", fmt.Errorf("取平台库 skill 归档失败: %w", e)
		}
		// 解压防炸弹校验（平台库归档同样校验，保持与原 Install 行为一致）
		if verr := validateArchiveSafety(d, "tar"); verr != nil {
			return nil, "", nil, "", ErrAppSkillArchiveTooDangerous
		}
		// 平台库归档由 PlatformSkillService 上传时已落到确定性 key，直接引用、无需回源/重写。
		rel := storage.LibrarySkillKey("platform", in.SourceRef, in.Version, "tar")
		return d, sh, map[string]any{"source": "platform", "name": in.SourceRef, "version": in.Version}, rel, nil
	case "clawhub":
		// ClawHub 来源：nil 时表示该来源未启用
		if s.clawhub == nil {
			return nil, "", nil, "", ErrAppSkillSourceUnknown
		}
		d, rel, e := s.cache.Fetch(ctx, "clawhub", in.SourceRef, in.Version, "zip", func(fctx context.Context) ([]byte, error) {
			b, derr := s.clawhub.Download(fctx, in.SourceRef, in.Version)
			if derr != nil {
				// 上游下载失败：包成上游不可用哨兵，供 handler 映射 502。
				return nil, fmt.Errorf("%w: %v", ErrSkillMarketUpstreamUnavailable, derr)
			}
			// 解压防炸弹校验放在回源闭包内：校验失败 → 返回错误 → 不写缓存。
			if verr := validateArchiveSafety(b, "zip"); verr != nil {
				return nil, ErrAppSkillArchiveTooDangerous
			}
			return b, nil
		})
		if e != nil {
			return nil, "", nil, "", e
		}
		return d, "", map[string]any{"source": "clawhub", "slug": in.SourceRef, "version": in.Version}, rel, nil
	default:
		// 未知来源类型，拒绝安装
		return nil, "", nil, "", ErrAppSkillSourceUnknown
	}
}
```

(e) `Install`：把取归档到落库这段改为用 `relPath`、删掉显式 `validateArchiveSafety` 与 `PutLibrarySkill`。即把原来这段：

```go
	archive, sha, meta, ext, err := s.fetchArchive(ctx, in)
	if err != nil {
		return AppSkillResult{}, err
	}
	if err := validateArchiveSafety(archive, ext); err != nil {
		return AppSkillResult{}, ErrAppSkillArchiveTooDangerous
	}
	if sha == "" {
		sum := sha256.Sum256(archive)
		sha = hex.EncodeToString(sum[:])
	}
	relPath, err := s.blobs.PutLibrarySkill(in.Source, in.SourceRef, in.Version, ext, archive)
	if err != nil {
		return AppSkillResult{}, err
	}
	metaJSON, _ := json.Marshal(meta)
```

替换为：

```go
	// 按来源取归档：clawhub 走读穿缓存（命中免回源），解压安全校验已在 fetchArchive 内按来源完成。
	archive, sha, meta, relPath, err := s.fetchArchive(ctx, in)
	if err != nil {
		return AppSkillResult{}, err
	}
	// clawhub 来源无预置 sha，本地计算
	if sha == "" {
		sum := sha256.Sum256(archive)
		sha = hex.EncodeToString(sum[:])
	}
	metaJSON, _ := json.Marshal(meta)
```

(f) `Update`：同理。把原来这段：

```go
	archive, sha, meta, ext, err := s.fetchArchive(ctx, in)
	if err != nil {
		return AppSkillResult{}, err
	}
	if err := validateArchiveSafety(archive, ext); err != nil {
		return AppSkillResult{}, ErrAppSkillArchiveTooDangerous
	}
	if sha == "" {
		sum := sha256.Sum256(archive)
		sha = hex.EncodeToString(sum[:])
	}
	relPath, err := s.blobs.PutLibrarySkill(row.Source, row.SourceRef, targetVersion, ext, archive)
	if err != nil {
		return AppSkillResult{}, err
	}
	metaJSON, _ := json.Marshal(meta)
```

替换为：

```go
	// 取目标版本归档（clawhub 走读穿缓存），安全校验已在 fetchArchive 内完成。
	archive, sha, meta, relPath, err := s.fetchArchive(ctx, in)
	if err != nil {
		return AppSkillResult{}, err
	}
	if sha == "" {
		sum := sha256.Sum256(archive)
		sha = hex.EncodeToString(sum[:])
	}
	metaJSON, _ := json.Marshal(meta)
```

注意：`Install`/`Update` 后续的 `CreateAppSkill` / `UpdateAppSkillVersion` 调用里的 `CachedTarPath: relPath`、`FileSize: int64(len(archive))` 等保持不变。

- [ ] **Step 4: 运行测试确认通过（含原有 Install/Update 用例不回归）**

Run: `go test ./internal/service/ -run 'TestAppSkillService' -v`
Expected: PASS——新增两个 clawhub 用例 + 原有 platform Install/Update/Reinstall/List 等全绿。

- [ ] **Step 5: 提交**

```bash
git add internal/service/app_skill_service.go internal/service/app_skill_service_test.go
git commit -m "feat(skill): 实例安装/更新 clawhub 归档走 S3 读穿缓存

fetchArchive 的 clawhub 分支改经 SkillArchiveCache：命中即免回源下载；
解压安全校验内移到回源闭包/平台分支，上游失败包成
ErrSkillMarketUpstreamUnavailable。Install/Update 改用缓存返回的相对路径，
去掉显式 PutLibrarySkill，保持单次写入。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: `AssistantVersionService.resolveLibrarySkill` clawhub 走 read-through

**Files:**
- Modify: `internal/service/assistant_version_service.go`（`resolveLibrarySkill` 的 clawhub 分支，约 606-626 行）
- Test: `internal/service/assistant_version_service_test.go`（新增上游失败测试）

说明：此处 `s.blobs` 已是 `LibraryBlobStore`，就地用 `NewSkillArchiveCache(s.blobs)` 包一层 read-through；命中即免回源；上游失败包成上游不可用哨兵。版本选取不做解压炸弹校验（保持与现有行为一致）。

- [ ] **Step 1: 写失败测试**

照搬现有 `TestAddSkillFromLibrary_ClawHub`（约 818 行）的真实构造，仅把 `fakeClawHub{archive:...}` 换成 `fakeClawHub{err:...}`。注意此处用的是同文件内的 `fakeLibBlob`（其 `OpenLibrarySkill` 返回 `(nil, nil)`，被 Task 1 的 `rc != nil` 守卫按未命中处理），**不是** `fakeLibraryBlob`。`fakeClawHub`（约 294 行）已支持 `err` 字段。在 `TestAddSkillFromLibrary_ClawHub` 之后追加：

```go
// TestAddSkillFromLibrary_ClawHub_UpstreamFail 助手版本加 clawhub skill 时上游下载失败：
// 返回的错误可被 errors.Is 识别为 ErrSkillMarketUpstreamUnavailable（供 handler 映射 502）。
func TestAddSkillFromLibrary_ClawHub_UpstreamFail(t *testing.T) {
	store := newFakeAVStore()
	id := seedVersion(store, "标准版", 1)
	blob := &fakeLibBlob{}
	// 上游下载器预置错误、缓存为空（fakeLibBlob.Open 返回 (nil,nil)→未命中）→ 回源失败。
	svc := NewAssistantVersionService(
		store, fakeImageResolver{}, fakeModelValidator{}, newFakePlatformSkillLibrary(),
		fakeClawHub{err: errors.New("clawhub 下载返回非 2xx: 502")}, blob,
	)
	_, err := svc.AddSkillFromLibrary(context.Background(), platformPrincipal(), id, AddSkillFromLibraryInput{
		Source: "clawhub", SourceRef: "self-improving", Name: "Self Improving", Version: "1.2.16",
	})
	// 上游故障须可被 errors.Is 识别为上游不可用。
	require.ErrorIs(t, err, ErrSkillMarketUpstreamUnavailable)
}
```

并确认 `assistant_version_service_test.go` 的 import 块含 `"errors"`（多数测试已用，通常已导入；缺则补）。

- [ ] **Step 2: 运行测试确认失败**

Run: `go test ./internal/service/ -run 'TestAddSkillFromLibrary_ClawHub' -v`
Expected: FAIL（当前 clawhub 分支返回的是 `fmt.Errorf("从 ClawHub 下载 skill 失败: %w", err)`，不是 `ErrSkillMarketUpstreamUnavailable`）。

- [ ] **Step 3: 改 `resolveLibrarySkill` 的 clawhub 分支**

把 `internal/service/assistant_version_service.go` 中 clawhub 分支（约 606-626 行）替换为：

```go
	case "clawhub":
		// 未配 ClawHub（downloader/blobs 任一为 nil）时拒绝，前端市场此时也不会展示 clawhub 条目。
		if s.clawhub == nil || s.blobs == nil {
			return AssistantVersionSkill{}, ErrAppSkillSourceUnknown
		}
		if in.Name == "" {
			return AssistantVersionSkill{}, fmt.Errorf("%w: clawhub 来源缺少 name", ErrAssistantVersionInvalid)
		}
		// 走读穿缓存：命中 library/clawhub/<slug>/<ver>.zip 即免回源；未命中回源下载并写回。
		cache := NewSkillArchiveCache(s.blobs)
		archive, relPath, err := cache.Fetch(ctx, "clawhub", in.SourceRef, in.Version, "zip", func(fctx context.Context) ([]byte, error) {
			b, derr := s.clawhub.Download(fctx, in.SourceRef, in.Version)
			if derr != nil {
				// 上游下载失败：包成上游不可用哨兵，供 handler 映射 502。
				return nil, fmt.Errorf("%w: %v", ErrSkillMarketUpstreamUnavailable, derr)
			}
			return b, nil
		})
		if err != nil {
			return AssistantVersionSkill{}, err
		}
		sum := sha256.Sum256(archive)
		return AssistantVersionSkill{
			Source: "clawhub", SourceRef: in.SourceRef, Name: in.Name, Version: in.Version,
			CachedPath: relPath, FileSize: int64(len(archive)), FileSha256: hex.EncodeToString(sum[:]),
		}, nil
```

- [ ] **Step 4: 运行测试确认通过（含原有成功用例不回归）**

Run: `go test ./internal/service/ -run 'TestAddSkillFromLibrary' -v`
Expected: PASS——新增上游失败用例绿；原 `TestAddSkillFromLibrary_ClawHub`（断言 `CachedPath == "library/clawhub/skill-vetter/1.0.0.zip"`）仍绿（缓存写回返回同一确定性 key）。

- [ ] **Step 5: 提交**

```bash
git add internal/service/assistant_version_service.go internal/service/assistant_version_service_test.go
git commit -m "feat(skill): 助手版本加 clawhub skill 走 S3 读穿缓存

resolveLibrarySkill 的 clawhub 分支改经 SkillArchiveCache：命中即免回源，
未命中回源写回；上游失败包成 ErrSkillMarketUpstreamUnavailable。CachedPath
仍为 library/clawhub/<slug>/<ver>.zip 确定性键。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: 全量校验（构建 / vet / 测试 / openapi）

**Files:** 无（仅运行校验命令）

- [ ] **Step 1: 全量构建 + vet**

Run: `go build ./... && go vet ./...`
Expected: 均无输出、退出码 0。

- [ ] **Step 2: 全量后端测试**

Run: `go test ./...`
Expected: 全 PASS（重点关注 `internal/service`、`internal/api/handlers`）。

- [ ] **Step 3: OpenAPI 同步校验（确认无契约漂移）**

Run: `make openapi-check`
Expected: 通过（本改动只新增错误分支与内部缓存，不改 handler 签名/请求体/响应类型/路由，yaml 应保持干净）。若失败，跑 `make openapi-gen && make web-types-gen` 并把变更纳入提交。

- [ ] **Step 4: 前端单测（确认无回归；前端无代码改动）**

Run: `cd web && npx vitest run`
Expected: 全 PASS（`apiDownload` 已解析 JSON 错误体并提取 `message`，502 文案自动透传到下载/安装 toast，无需改前端）。

- [ ] **Step 5: 如有未提交的生成产物则提交**

仅当 Step 3 产生了 yaml/types 变更时：

```bash
git add openapi/openapi.yaml web/src/api/generated.ts
git commit -m "chore(openapi): 同步 skill 归档缓存改动后的生成产物"
```

否则跳过本步。

---

## Task 7: 真实浏览器验证（AGENTS.md 交付前要求）

**Files:** 无（手工 + 浏览器，按需重建本地镜像）

前置：本地 k3d 在跑（`rtk proxy kubectl get pods -n ocm` 全 Running）。本改动只动 manager-api，需 `make local-build`（或等价仅重建 manager-api 镜像并 `kubectl rollout restart deploy/manager-api -n ocm`）后验证。排查 k8s 一律用 `rtk proxy kubectl` 绕过 rtk 压缩。

- [ ] **Step 1: 部署改动到本地 k3d**

重建并滚动更新 manager-api，等待 `kubectl rollout status deploy/manager-api -n ocm` 就绪。

- [ ] **Step 2: 验证错误处理（self-improving，上游 502）**

浏览器（chrome-devtools MCP）登录 `http://ocm.localhost`（admin/admin123），进 `apps/<id>/skills` → 技能市场 → 打开 self-improving 详情 → 点「下载」：
- 期望：toast 显示「上游技能市场暂时不可用，请稍后重试」，**不再**是「服务器内部错误」。
- 网络面板：`/skill-market/download?...&ref=self-improving...` 返回 **502**（非 500）。
- 点 self-improving「安装」：toast 同样为明确的上游不可用文案，`POST .../skills` 返回 **502**。

- [ ] **Step 3: 验证缓存命中（weather 等正常 skill）**

- 打开一个正常 skill（如 weather）详情 → 点「下载」成功（浏览器下载到 zip）。
- 去 MinIO 确认对象已写入：`library/clawhub/weather/<version>.zip` 存在（可经 minio console 或 `rtk proxy kubectl exec` mc 客户端确认）。
- 再次「下载」/「安装」该 skill：确认走缓存——manager-api 日志在第二次操作时无对该 skill 的上游 `clawhubcn.com/api/v1/download` 请求（或：临时令上游不可达后该 skill 仍能从缓存安装成功）。

- [ ] **Step 4: 验证安装端到端（正常 skill）**

装一个正常 clawhub skill → hermes reload → 「已安装」列表对账为 active（沿用既有验证口径，三角色按需复核 org_admin 安装路径）。

- [ ] **Step 5: 验证结论记录**

把逐项验证结果（含网络状态码、MinIO 对象、对账状态的证据）写入交付说明。若任一项不通过，回到对应 Task 修复并重验。

---

## Self-Review

- **Spec coverage：**
  - 目标①错误处理修复 → Task 2（哨兵 + 502）+ Task 3/4/5（三个回源点包装上游错误）+ Task 7 Step 2 验证。✓
  - 目标②归档 S3 缓存（下载/安装/版本）→ Task 1（缓存）+ Task 3（市场下载）+ Task 4（安装/更新）+ Task 5（版本）+ Task 7 Step 3 验证。✓
  - 目标③通用化 → Task 1 的 `SkillArchiveCache` 按 `(source, ref, version, ext)` 抽象寻址，新市场只需传回源闭包；spec「非目标/决策」均落到对应 Task。✓
  - platform 不回源保持不变 → Task 4 platform 分支直接引用确定性 key、不经缓存回源。✓
  - 零 DB 迁移 / 无 OpenAPI 变更 / 前端零改码 → Task 6 校验。✓
- **Placeholder 扫描：** 无占位。各 Task 均为可直接落地的完整代码与命令；Task 5 已用 `TestAddSkillFromLibrary_ClawHub` 的真实构造（`NewAssistantVersionService(...)` + `fakeClawHub{err:...}` + `fakeLibBlob`）。注意区分两个同包 fake：`fakeLibraryBlob`（带 `stored`、未命中返 error，用于 Task 1/4）与 `fakeLibBlob`（`Open` 返 `(nil,nil)`，用于 Task 5，靠 `rc != nil` 守卫处理）。
- **类型一致性：** `SkillArchiveCache.Fetch(ctx, source, ref, version, ext, fetch) (data, relPath, err)`、`NewSkillArchiveCache(blobs)`、`NewClawHubSource(api, rdb, ttl, cache)`、`fetchArchive(...) (data, sha, meta, relPath, err)`、`ErrSkillMarketUpstreamUnavailable` 在各 Task 间签名/命名一致；缓存键 `library/<source>/<ref>/<version>.<ext>` 与 `storage.LibrarySkillKey` 及 `fakeLibraryBlob` 命名规则一致。
