# 企业管理员双视角 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 org_admin 增加「企业管理 / 我的实例」视角切换,使其能管理自己的实例并安装仅管理员可见的定制技能。

**Architecture:** 纯前端实现(后端已就绪,零改动)。新增一个 `useAdminPerspective` composable(localStorage 持久化视角);`DashboardLayout` 把当前用户自有实例的数据源从 `useMemberApp` 统一到 `useOwnApp`(对成员行为不变),并按「角色 × 视角」分支渲染菜单、在顶栏渲染仅 org_admin 可见的视角切换器;「我的实例」视角复用成员现有路由/页面;无实例时 `AppEmptyPage` 给 org_admin 一个跳成员页自助建实例的入口。

**Tech Stack:** Vue 3 (script setup) + TypeScript + Pinia + Naive UI + Vitest;路由 vue-router;图标 lucide-vue-next。

---

## 背景事实(实现前必读)

- **后端零改动**。已读码确认:`ListByOrg` 对 org_admin 返回本企业全部实例(含自有);`CanCreateAppForMember/CanViewApp/CanManageApp/CanSwitchAppVersion` 对 admin 自有实例全放行;一人一实例唯一约束不分角色。设计文档:`docs/superpowers/specs/2026-06-11-管理员双视角-design.md`。
- **路由实况**(`web/src/app/router.ts`):`/apps/:appId/:tab`(tab 含 overview/kanban/cron/channels/knowledge/skills/workspace/audit,其中 runtime 仅 platform)、`/skills`(无角色限制,组件 `OrgSkillsPage`)、`/members`(allowedRoles 含 org_admin)、`/apps/empty`(组件 `AppEmptyPage`)均对 org_admin 可访问。
- **成员自助建实例入口**:`/members` 页(`MembersPage.vue`)中,org_admin 在自己那一行用「为该成员创建实例」(行内表单)完成建实例。空状态「去成员页创建」按钮跳 `/members` 即可。
- **`useOwnApp`**(`web/src/composables/useOwnApp.ts`)已存在,对 org_member 与 org_admin 都启用查询、返回 `{ appId, hasApp, isLoading, app }`,数据语义与 `useMemberApp` 对成员完全一致。
- **测试范式**(`web/src/layouts/DashboardLayout.spec.ts`):不使用 `createTestingPinia`,而是 `vi.hoisted` + `vi.mock` 手动桩 store / composable / 路由;Naive UI 组件用自定义 stub;菜单断言用 `menuLabels()/menuKeys()` 从 `[data-test="menu-item"]` 提取。composable 测试放 `web/src/composables/__tests__/`。
- **运行测试**:`cd web && npx vitest run <文件>`(`package.json` script 为 `vitest`)。

---

## File Structure

- **Create** `web/src/composables/useAdminPerspective.ts` — org_admin 视角状态(localStorage 持久化),职责单一:读/写/清当前视角。
- **Create** `web/src/composables/__tests__/useAdminPerspective.spec.ts` — 上述 composable 单测。
- **Modify** `web/src/layouts/DashboardLayout.vue` — 数据源换 `useOwnApp`;接入 `useAdminPerspective`;菜单按视角分支;顶栏视角切换器;退出登录清视角。
- **Modify** `web/src/layouts/DashboardLayout.spec.ts` — 更新 composable 桩(useMemberApp→useOwnApp、新增 useAdminPerspective);新增视角分支与切换器用例。
- **Modify** `web/src/pages/apps/AppEmptyPage.vue` — org_admin 显示「去成员页创建实例」按钮。
- **Create** `web/src/pages/apps/AppEmptyPage.spec.ts` — 空状态页按角色渲染的单测。

---

## Task 1: useAdminPerspective composable

**Files:**
- Create: `web/src/composables/useAdminPerspective.ts`
- Test: `web/src/composables/__tests__/useAdminPerspective.spec.ts`

- [ ] **Step 1: 写失败测试**

创建 `web/src/composables/__tests__/useAdminPerspective.spec.ts`:

```ts
import { beforeEach, describe, expect, it } from 'vitest'

import { useAdminPerspective } from '../useAdminPerspective'

describe('useAdminPerspective', () => {
  // 每个用例前清空持久化,避免 localStorage 在用例间互相污染
  beforeEach(() => {
    localStorage.clear()
  })

  // 无持久化值时默认落「企业管理」视角(manage)
  it('defaults to manage when nothing stored', () => {
    const { perspective } = useAdminPerspective()
    expect(perspective.value).toBe('manage')
  })

  // setPerspective 切到实例视角并写 localStorage;模拟刷新(新建实例)应恢复该视角
  it('persists perspective to localStorage', () => {
    const { setPerspective } = useAdminPerspective()
    setPerspective('instance')
    expect(localStorage.getItem('oc.admin.perspective')).toBe('instance')
    expect(useAdminPerspective().perspective.value).toBe('instance')
  })

  // resetPerspective 清持久化并回默认(退出登录场景)
  it('clears persistence on reset', () => {
    const { setPerspective, resetPerspective, perspective } = useAdminPerspective()
    setPerspective('instance')
    resetPerspective()
    expect(perspective.value).toBe('manage')
    expect(localStorage.getItem('oc.admin.perspective')).toBeNull()
  })

  // 非法持久化值回退默认,避免脏数据污染视角
  it('falls back to manage on invalid stored value', () => {
    localStorage.setItem('oc.admin.perspective', 'bogus')
    expect(useAdminPerspective().perspective.value).toBe('manage')
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd web && npx vitest run src/composables/__tests__/useAdminPerspective.spec.ts`
Expected: FAIL — 模块 `../useAdminPerspective` 不存在。

- [ ] **Step 3: 写实现**

创建 `web/src/composables/useAdminPerspective.ts`:

```ts
// useAdminPerspective 维护企业管理员(org_admin)的「视角」状态并持久化到 localStorage。
// 视角决定 DashboardLayout 渲染哪一套菜单:'manage'=企业管理视角,'instance'=我的实例视角。
// 仅 org_admin 真正消费此状态;其余角色即使读到默认值也无副作用(菜单分支不依赖)。
import { ref } from 'vue'

// AdminPerspective:两种视角的字面量类型。
export type AdminPerspective = 'manage' | 'instance'

// STORAGE_KEY:持久化键名。带 oc. 前缀避免与其他业务键冲突。
const STORAGE_KEY = 'oc.admin.perspective'

// readPerspective:从 localStorage 读取视角;无值或非法值统一回退到默认的企业管理视角。
function readPerspective(): AdminPerspective {
  return localStorage.getItem(STORAGE_KEY) === 'instance' ? 'instance' : 'manage'
}

export function useAdminPerspective() {
  // perspective:当前视角的响应式状态,初值取自持久化(支持刷新保持)。
  const perspective = ref<AdminPerspective>(readPerspective())

  // setPerspective:切换视角并写持久化,保证刷新后保持同一视角。
  function setPerspective(next: AdminPerspective) {
    perspective.value = next
    localStorage.setItem(STORAGE_KEY, next)
  }

  // resetPerspective:清除持久化并回默认(退出登录时调用),避免换账号串视角。
  function resetPerspective() {
    perspective.value = 'manage'
    localStorage.removeItem(STORAGE_KEY)
  }

  return { perspective, setPerspective, resetPerspective }
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd web && npx vitest run src/composables/__tests__/useAdminPerspective.spec.ts`
Expected: PASS(4 个用例全过)。

- [ ] **Step 5: 提交**

```bash
git add web/src/composables/useAdminPerspective.ts web/src/composables/__tests__/useAdminPerspective.spec.ts
git commit -m "feat(web): 新增 useAdminPerspective 视角状态 composable

为 org_admin 双视角功能提供 manage/instance 视角状态,localStorage 持久化、
非法值回退默认、退出登录可清除。"
```

---

## Task 2: DashboardLayout 自有实例数据源切到 useOwnApp(行为不变的前置重构)

把 `DashboardLayout` 中驱动「当前用户自有实例」的数据源从 `useMemberApp` 换成 `useOwnApp`,使后续 admin 实例视角能复用同一套实例菜单逻辑。对 org_member 行为完全不变(两者对成员返回相同数据)。

**Files:**
- Modify: `web/src/layouts/DashboardLayout.vue`(import 与 `useMemberApp()` 调用处、`memberAppId/memberHasApp` 变量)
- Modify: `web/src/layouts/DashboardLayout.spec.ts`(composable 桩路径)

- [ ] **Step 1: 改测试桩为 useOwnApp**

在 `web/src/layouts/DashboardLayout.spec.ts` 中,把 `memberAppState` 桩改为桩 `useOwnApp`。

将第 37-39 行:

```ts
vi.mock('@/composables/useMemberApp', () => ({
  useMemberApp: () => memberAppState,
}))
```

改为:

```ts
vi.mock('@/composables/useOwnApp', () => ({
  useOwnApp: () => memberAppState,
}))
```

(`memberAppState` 变量名保留不动,仅换 mock 的模块与导出名。)

- [ ] **Step 2: 跑测试确认失败**

Run: `cd web && npx vitest run src/layouts/DashboardLayout.spec.ts`
Expected: FAIL — `DashboardLayout.vue` 仍 import `useMemberApp`,真实 `useMemberApp`(内部用 `useAppsByOrgQuery` 需 QueryClient)被实例化导致报错或成员菜单用例断言失败。

- [ ] **Step 3: 改实现源文件**

在 `web/src/layouts/DashboardLayout.vue`:

把第 155 行 import:

```ts
import { useMemberApp } from '@/composables/useMemberApp'
```

改为:

```ts
import { useOwnApp } from '@/composables/useOwnApp'
```

把第 218 行:

```ts
const { appId: memberAppId, hasApp: memberHasApp } = useMemberApp()
```

改为:

```ts
const { appId: memberAppId, hasApp: memberHasApp } = useOwnApp()
```

(变量名 `memberAppId/memberHasApp` 暂保留,Task 3 再随菜单分支一起语义化;本步只换数据源,不改任何行为。)

- [ ] **Step 4: 跑测试确认通过**

Run: `cd web && npx vitest run src/layouts/DashboardLayout.spec.ts`
Expected: PASS(原有全部用例不变通过,证明成员行为未回归)。

- [ ] **Step 5: 提交**

```bash
git add web/src/layouts/DashboardLayout.vue web/src/layouts/DashboardLayout.spec.ts
git commit -m "refactor(web): DashboardLayout 自有实例数据源统一到 useOwnApp

把侧边栏自有实例数据源从 useMemberApp 换成 useOwnApp(对成员返回相同数据、
行为不变),为 org_admin 实例视角复用同一套实例菜单做准备。"
```

---

## Task 3: 菜单与高亮按「角色 × 视角」分支

接入 `useAdminPerspective`,新增 `inOwnInstanceView` 判定,使「我的实例」菜单(成员同款)同时服务 org_member 与 org_admin(instance 视角);org_admin 的「企业管理」菜单去掉独立「技能」项。

**Files:**
- Modify: `web/src/layouts/DashboardLayout.vue`(import、`inOwnInstanceView`、`activeKey`、`memberAppTabPath`、`menuOptions`)
- Modify: `web/src/layouts/DashboardLayout.spec.ts`(新增 useAdminPerspective 桩与视角分支用例)

- [ ] **Step 1: 新增视角桩与失败测试**

在 `web/src/layouts/DashboardLayout.spec.ts`,在已有 `memberAppState`(第 21-25 行)之后新增视角桩(用 `vi.hoisted`):

```ts
const setPerspective = vi.hoisted(() => vi.fn())
const resetPerspective = vi.hoisted(() => vi.fn())
const adminPerspectiveState = vi.hoisted(() => ({
  perspective: { value: 'manage' as 'manage' | 'instance' },
  setPerspective,
  resetPerspective,
}))
```

在已有 `vi.mock('@/composables/useOwnApp', ...)` 之后新增:

```ts
vi.mock('@/composables/useAdminPerspective', () => ({
  useAdminPerspective: () => adminPerspectiveState,
}))
```

在 `beforeEach`(第 180-194 行)末尾追加重置:

```ts
    adminPerspectiveState.perspective.value = 'manage'
    setPerspective.mockReset()
    resetPerspective.mockReset()
    // setPerspective 桩实现:更新视角 ref,使菜单在切换后重新计算
    setPerspective.mockImplementation((next: 'manage' | 'instance') => {
      adminPerspectiveState.perspective.value = next
    })
```

在 `describe` 内新增三个用例(放在「renames organization knowledge entry for non-member menus」用例之后):

```ts
  // 覆盖 org_admin 企业管理视角菜单:管理项齐全,且「技能」已迁出(不在此视角)。
  it('renders management menu without skills for org_admin manage perspective', () => {
    routeState.path = '/'
    authState.user = { id: 'org-admin-1', username: 'owner', display_name: '管理员', role: 'org_admin', org_id: 'org-1' }
    authState.isPlatformAdmin = false
    authState.isOrgAdmin = true
    authState.isOrgMember = false
    adminPerspectiveState.perspective.value = 'manage'

    const wrapper = mountLayout()

    expect(menuLabels(wrapper)).toEqual(['总览', '成员', '实例', '企业知识库', '账户余额', '审计', '用量'])
    expect(menuLabels(wrapper)).not.toContain('技能')
  })

  // 覆盖 org_admin 我的实例视角菜单:与组织成员同款(含「技能」),由自有实例 appId 驱动。
  it('renders member-style menu for org_admin instance perspective', () => {
    routeState.path = '/apps/admin-app/overview'
    authState.user = { id: 'org-admin-1', username: 'owner', display_name: '管理员', role: 'org_admin', org_id: 'org-1' }
    authState.isPlatformAdmin = false
    authState.isOrgAdmin = true
    authState.isOrgMember = false
    adminPerspectiveState.perspective.value = 'instance'
    memberAppState.appId.value = 'admin-app'
    memberAppState.hasApp.value = true

    const wrapper = mountLayout()

    expect(menuLabels(wrapper)).toEqual(['总览', '渠道', '工作目录', '个人知识库', '企业知识库', '技能', '任务', '定时任务', '用量'])
    expect(menuKeys(wrapper)).toEqual([
      '/apps/admin-app/overview',
      '/apps/admin-app/channels',
      '/apps/admin-app/workspace',
      '/apps/admin-app/knowledge',
      '/knowledge',
      '/skills',
      '/apps/admin-app/kanban',
      '/apps/admin-app/cron',
      '/usage',
    ])
  })

  // 覆盖 org_admin 实例视角当前高亮:在 /apps/:id/:tab 上应高亮对应实例 tab(复用成员高亮逻辑)。
  it('highlights instance tab for org_admin instance perspective', () => {
    routeState.path = '/apps/admin-app/channels'
    authState.user = { id: 'org-admin-1', username: 'owner', display_name: '管理员', role: 'org_admin', org_id: 'org-1' }
    authState.isPlatformAdmin = false
    authState.isOrgAdmin = true
    authState.isOrgMember = false
    adminPerspectiveState.perspective.value = 'instance'
    memberAppState.appId.value = 'admin-app'
    memberAppState.hasApp.value = true

    const wrapper = mountLayout()

    expect(wrapper.find('[data-test="menu"]').attributes('data-value')).toBe('/apps/admin-app/channels')
  })
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd web && npx vitest run src/layouts/DashboardLayout.spec.ts`
Expected: FAIL — `useAdminPerspective` 模块桩对应的源文件未在组件中引用;`org_admin manage` 菜单仍含「技能」、`org_admin instance` 仍走管理菜单。

- [ ] **Step 3: 接入视角并分支菜单**

在 `web/src/layouts/DashboardLayout.vue`:

(a)在 import 区(`useOwnApp` import 之后)新增:

```ts
import { useAdminPerspective } from '@/composables/useAdminPerspective'
```

(b)在 `const { appId: memberAppId, hasApp: memberHasApp } = useOwnApp()`(第 218 行)之后新增:

```ts
// 视角状态:仅 org_admin 消费,决定渲染企业管理菜单还是我的实例菜单。Task 4 再接入切换器与退出清除。
const { perspective } = useAdminPerspective()

// inOwnInstanceView:当前是否处于「以自有实例为中心」的视角。
// org_member 恒为 true;org_admin 仅在 instance 视角为 true;platform_admin 恒为 false。
const inOwnInstanceView = computed(() => isOrgMember.value || (isOrgAdmin.value && perspective.value === 'instance'))
```

(c)把 `activeKey`(第 178-205 行)中两处 `isOrgMember.value` 判定改为 `inOwnInstanceView.value`:

第 180 行:

```ts
  if (p === '/') return isOrgMember.value ? memberAppTabKey('overview') : '/'
```

改为:

```ts
  if (p === '/') return inOwnInstanceView.value ? memberAppTabKey('overview') : '/'
```

第 182 行:

```ts
  if (isOrgMember.value && p.startsWith('/apps')) {
```

改为:

```ts
  if (inOwnInstanceView.value && p.startsWith('/apps')) {
```

(d)把 `memberAppTabPath`(第 227-231 行)的角色守卫从 `isOrgMember` 改为 `inOwnInstanceView`:

第 228 行:

```ts
  if (!isOrgMember.value) return '/apps'
```

改为:

```ts
  if (!inOwnInstanceView.value) return '/apps'
```

(e)把 `menuOptions`(第 244-308 行)的成员分支条件从 `isOrgMember.value` 改为 `inOwnInstanceView.value`,使 org_admin 实例视角复用同一套实例菜单。

第 245 行:

```ts
  if (isOrgMember.value) {
```

改为:

```ts
  if (inOwnInstanceView.value) {
```

(f)删除企业管理菜单里 org_admin 的独立「技能」项(第 291-294 行整段):

```ts
  // 技能:org_admin 与成员对称,给同一顶级技能页(key /skills、同图标),
  // 管理自己的实例技能并可提交/查看定制技能工单。平台管理员另有 /platform/skills 入口,不在此处加。
  if (isOrgAdmin.value) {
    items.push({ key: '/skills', label: '技能', icon: () => h(Puzzle, { size: 18 }) })
  }
```

整段删除(「技能」已在 inOwnInstanceView 实例菜单中提供;`Puzzle` 图标仍被实例菜单使用,import 保留)。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd web && npx vitest run src/layouts/DashboardLayout.spec.ts`
Expected: PASS(原有成员/平台用例 + 新增 3 个 org_admin 视角用例全过)。

- [ ] **Step 5: 提交**

```bash
git add web/src/layouts/DashboardLayout.vue web/src/layouts/DashboardLayout.spec.ts
git commit -m "feat(web): DashboardLayout 菜单按 org_admin 视角分支

接入 useAdminPerspective,新增 inOwnInstanceView 判定:org_admin instance 视角
复用成员同款实例菜单(含技能),manage 视角渲染管理菜单并去掉独立技能项。
activeKey 与 memberAppTabPath 同步改用 inOwnInstanceView,使实例 tab 高亮对
org_admin 实例视角同样生效。"
```

---

## Task 4: 顶栏视角切换器 + 切换导航 + 退出清视角

在顶栏渲染仅 org_admin 可见的 `[企业管理 | 我的实例]` 切换器;切换时持久化并导航到目标视角首页;退出登录清除视角。

**Files:**
- Modify: `web/src/layouts/DashboardLayout.vue`(模板顶栏、`perspective` 解构补全、`onSwitchPerspective`、`onLogout`)
- Modify: `web/src/layouts/DashboardLayout.spec.ts`(切换器可见性与切换导航用例)

- [ ] **Step 1: 写失败测试**

在 `web/src/layouts/DashboardLayout.spec.ts` 末尾(`describe` 内)新增用例:

```ts
  // 覆盖切换器可见性:仅 org_admin 渲染视角切换;platform_admin 不渲染。
  it('shows perspective switch only for org_admin', () => {
    authState.user = { id: 'org-admin-1', username: 'owner', display_name: '管理员', role: 'org_admin', org_id: 'org-1' }
    authState.isPlatformAdmin = false
    authState.isOrgAdmin = true
    authState.isOrgMember = false

    const adminWrapper = mountLayout()
    const adminButtons = adminWrapper.findAll('button').map(b => b.text().trim())
    expect(adminButtons).toContain('企业管理')
    expect(adminButtons).toContain('我的实例')

    authState.isOrgAdmin = false
    authState.isPlatformAdmin = true
    authState.user = { id: 'admin-1', username: 'admin', display_name: 'admin', role: 'platform_admin', org_id: 'org-1' }
    const platformWrapper = mountLayout()
    const platformButtons = platformWrapper.findAll('button').map(b => b.text().trim())
    expect(platformButtons).not.toContain('我的实例')
  })

  // 覆盖切到我的实例视角(无自有实例):应持久化视角并导航到空状态页。
  it('switches to instance perspective and navigates to empty state when no own app', async () => {
    authState.user = { id: 'org-admin-1', username: 'owner', display_name: '管理员', role: 'org_admin', org_id: 'org-1' }
    authState.isPlatformAdmin = false
    authState.isOrgAdmin = true
    authState.isOrgMember = false
    memberAppState.appId.value = undefined
    memberAppState.hasApp.value = false

    const wrapper = mountLayout()
    const instanceButton = wrapper.findAll('button').find(b => b.text().trim() === '我的实例')
    await instanceButton!.trigger('click')

    expect(setPerspective).toHaveBeenCalledWith('instance')
    expect(routerPush).toHaveBeenCalledWith('/apps/empty')
  })

  // 覆盖切到我的实例视角(有自有实例):应导航到自有实例总览。
  it('switches to instance perspective and navigates to own app overview', async () => {
    authState.user = { id: 'org-admin-1', username: 'owner', display_name: '管理员', role: 'org_admin', org_id: 'org-1' }
    authState.isPlatformAdmin = false
    authState.isOrgAdmin = true
    authState.isOrgMember = false
    memberAppState.appId.value = 'admin-app'
    memberAppState.hasApp.value = true

    const wrapper = mountLayout()
    const instanceButton = wrapper.findAll('button').find(b => b.text().trim() === '我的实例')
    await instanceButton!.trigger('click')

    expect(routerPush).toHaveBeenCalledWith('/apps/admin-app/overview')
  })

  // 覆盖切回企业管理视角:应持久化并导航到管理总览根路由。
  it('switches back to manage perspective and navigates to root', async () => {
    authState.user = { id: 'org-admin-1', username: 'owner', display_name: '管理员', role: 'org_admin', org_id: 'org-1' }
    authState.isPlatformAdmin = false
    authState.isOrgAdmin = true
    authState.isOrgMember = false
    adminPerspectiveState.perspective.value = 'instance'

    const wrapper = mountLayout()
    const manageButton = wrapper.findAll('button').find(b => b.text().trim() === '企业管理')
    await manageButton!.trigger('click')

    expect(setPerspective).toHaveBeenCalledWith('manage')
    expect(routerPush).toHaveBeenCalledWith('/')
  })

  // 覆盖退出登录:除清登录态外还需清除视角持久化,避免换账号串视角。
  it('resets perspective on logout', async () => {
    authState.user = { id: 'org-admin-1', username: 'owner', display_name: '管理员', role: 'org_admin', org_id: 'org-1' }
    authState.isPlatformAdmin = false
    authState.isOrgAdmin = true
    authState.isOrgMember = false
    logout.mockResolvedValue(undefined)

    const wrapper = mountLayout()
    const logoutButton = wrapper.findAll('button').find(b => b.text().includes('退出'))
    await logoutButton!.trigger('click')
    await flushPromises()

    expect(resetPerspective).toHaveBeenCalled()
  })
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd web && npx vitest run src/layouts/DashboardLayout.spec.ts`
Expected: FAIL — 顶栏无「企业管理/我的实例」按钮;`onSwitchPerspective`、退出清视角均未实现。

- [ ] **Step 3: 实现切换器与动作**

在 `web/src/layouts/DashboardLayout.vue`:

(a)把 Task 3 中 `const { perspective } = useAdminPerspective()` 改为解构补全:

```ts
const { perspective, setPerspective, resetPerspective } = useAdminPerspective()
```

(b)在模板顶栏 `<n-layout-header>` 内,把标题块与 `topbar-actions` 之间插入切换器。将现有(第 64-78 行附近):

```html
        <div>
          <p class="eyebrow">{{ environmentLabel }}</p>
          <h1 style="margin: 0; font-size: 20px">控制台</h1>
        </div>
        <div class="topbar-actions">
```

改为(在两个 div 之间插入切换器):

```html
        <div>
          <p class="eyebrow">{{ environmentLabel }}</p>
          <h1 style="margin: 0; font-size: 20px">控制台</h1>
        </div>
        <!-- 视角切换器:仅 org_admin 可见,在「企业管理」与「我的实例」两视角间切换并导航。 -->
        <div v-if="isOrgAdmin" class="perspective-switch">
          <n-button
            size="small"
            :type="perspective === 'manage' ? 'primary' : 'default'"
            @click="onSwitchPerspective('manage')"
          >
            企业管理
          </n-button>
          <n-button
            size="small"
            :type="perspective === 'instance' ? 'primary' : 'default'"
            @click="onSwitchPerspective('instance')"
          >
            我的实例
          </n-button>
        </div>
        <div class="topbar-actions">
```

(c)在 `<script setup>` 中,新增 `AdminPerspective` 类型 import(与 composable 同源):

把:

```ts
import { useAdminPerspective } from '@/composables/useAdminPerspective'
```

改为:

```ts
import { useAdminPerspective, type AdminPerspective } from '@/composables/useAdminPerspective'
```

(d)在 `onLogout`(第 316-319 行)开头加清视角:

```ts
async function onLogout() {
  await auth.logout()
  await router.replace('/login')
}
```

改为:

```ts
async function onLogout() {
  // 退出前清除视角持久化,避免下个登录用户(可能不同 org/角色)沿用上一个视角。
  resetPerspective()
  await auth.logout()
  await router.replace('/login')
}
```

(e)在 `onLogout` 之前新增 `onSwitchPerspective`:

```ts
// onSwitchPerspective:切换 org_admin 视角并导航到目标视角的落地页。
// 切到我的实例:有自有实例进总览,无实例进空状态页(由 AppEmptyPage 给自助建实例入口)。
// 切回企业管理:回管理总览根路由。
function onSwitchPerspective(view: AdminPerspective) {
  setPerspective(view)
  if (view === 'instance') {
    router.push(memberHasApp.value && memberAppId.value ? `/apps/${memberAppId.value}/overview` : '/apps/empty')
    return
  }
  router.push('/')
}
```

(f)在 `<style scoped>` 末尾新增切换器样式:

```css
/* 视角切换器:两个分段按钮紧贴,放在顶栏标题与右侧操作之间。 */
.perspective-switch {
  display: flex;
  gap: 4px;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd web && npx vitest run src/layouts/DashboardLayout.spec.ts`
Expected: PASS(新增 5 个用例 + 既有用例全过)。

- [ ] **Step 5: 提交**

```bash
git add web/src/layouts/DashboardLayout.vue web/src/layouts/DashboardLayout.spec.ts
git commit -m "feat(web): 顶栏 org_admin 视角切换器与切换导航

仅 org_admin 渲染「企业管理 / 我的实例」切换器;切到实例视角导航到自有实例总览
(无实例则空状态页),切回管理视角回根路由;退出登录清除视角持久化。"
```

---

## Task 5: AppEmptyPage 给 org_admin 自助建实例入口

无实例时,`AppEmptyPage` 对 org_admin 显示「去成员页创建实例」按钮(跳 `/members`),对其他角色保持「请联系管理员创建实例」。

**Files:**
- Modify: `web/src/pages/apps/AppEmptyPage.vue`
- Test: `web/src/pages/apps/AppEmptyPage.spec.ts`

- [ ] **Step 1: 写失败测试**

创建 `web/src/pages/apps/AppEmptyPage.spec.ts`:

```ts
import { mount } from '@vue/test-utils'
import { defineComponent, h } from 'vue'
import { beforeEach, describe, expect, it, vi } from 'vitest'

import AppEmptyPage from './AppEmptyPage.vue'

const routerPush = vi.hoisted(() => vi.fn())
const authState = vi.hoisted(() => ({
  isOrgAdmin: false,
}))

vi.mock('vue-router', () => ({
  useRouter: () => ({ push: routerPush }),
}))

vi.mock('@/stores/auth', () => ({
  useAuthStore: () => authState,
}))

// EmptyStub 暴露 description 与 extra 插槽,便于断言文案与按钮渲染。
const EmptyStub = defineComponent({
  props: ['description'],
  setup(props, { slots }) {
    return () => h('div', { 'data-test': 'empty', 'data-desc': props.description }, [
      slots.icon?.(),
      slots.extra?.(),
    ])
  },
})

const ButtonStub = defineComponent({
  emits: ['click'],
  setup(_, { slots, emit }) {
    return () => h('button', { onClick: () => emit('click') }, slots.default?.())
  },
})

function mountPage() {
  return mount(AppEmptyPage, {
    global: {
      stubs: {
        NEmpty: EmptyStub, 'n-empty': EmptyStub,
        NButton: ButtonStub, 'n-button': ButtonStub,
        Bot: { template: '<i />' },
      },
    },
  })
}

describe('AppEmptyPage', () => {
  beforeEach(() => {
    routerPush.mockClear()
    authState.isOrgAdmin = false
  })

  // org_member(非管理员):仅提示联系管理员,无建实例按钮。
  it('shows contact-admin hint without create button for non-admin', () => {
    const wrapper = mountPage()
    expect(wrapper.find('[data-test="empty"]').attributes('data-desc')).toBe('请联系管理员创建实例')
    expect(wrapper.findAll('button').length).toBe(0)
  })

  // org_admin:显示自助建实例入口,点击跳成员页。
  it('shows self-service create entry for org_admin', async () => {
    authState.isOrgAdmin = true
    const wrapper = mountPage()
    expect(wrapper.find('[data-test="empty"]').attributes('data-desc')).toBe('你还没有属于自己的实例')
    const button = wrapper.findAll('button').find(b => b.text().includes('去成员页创建'))
    expect(button).toBeTruthy()
    await button!.trigger('click')
    expect(routerPush).toHaveBeenCalledWith('/members')
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd web && npx vitest run src/pages/apps/AppEmptyPage.spec.ts`
Expected: FAIL — 当前组件描述恒为「请联系管理员创建实例」、无按钮,org_admin 用例失败。

- [ ] **Step 3: 改实现**

把 `web/src/pages/apps/AppEmptyPage.vue` 整体替换为:

```vue
<!-- web/src/pages/apps/AppEmptyPage.vue -->
<template>
  <div class="empty-container">
    <n-empty :description="emptyDescription">
      <template #icon>
        <Bot :size="48" :stroke-width="1" />
      </template>
      <!-- org_admin 可自助建实例:给跳成员页的入口;其余角色仅提示联系管理员,不展示按钮。 -->
      <template v-if="auth.isOrgAdmin" #extra>
        <n-button size="small" @click="goCreate">去成员页创建实例</n-button>
      </template>
    </n-empty>
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import { useRouter } from 'vue-router'
import { NButton, NEmpty } from 'naive-ui'
import { Bot } from 'lucide-vue-next'

import { useAuthStore } from '@/stores/auth'

// 空状态页同时服务 org_member(无实例,需联系管理员)与 org_admin(可自助建实例)。
const auth = useAuthStore()
const router = useRouter()

// emptyDescription:文案随角色变化,管理员可自助创建、成员需联系管理员。
const emptyDescription = computed(() =>
  auth.isOrgAdmin ? '你还没有属于自己的实例' : '请联系管理员创建实例',
)

// goCreate:跳成员页;org_admin 在自己那一行用「为该成员创建实例」完成自助建实例。
function goCreate() {
  router.push('/members')
}
</script>

<style scoped>
.empty-container {
  display: flex;
  align-items: center;
  justify-content: center;
  flex: 1;
  min-height: 400px;
}
</style>
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd web && npx vitest run src/pages/apps/AppEmptyPage.spec.ts`
Expected: PASS(2 个用例全过)。

- [ ] **Step 5: 提交**

```bash
git add web/src/pages/apps/AppEmptyPage.vue web/src/pages/apps/AppEmptyPage.spec.ts
git commit -m "feat(web): 空状态页为 org_admin 提供自助建实例入口

AppEmptyPage 对 org_admin 显示「去成员页创建实例」按钮(跳 /members 自助建),
其余角色保持「请联系管理员创建实例」文案、不展示按钮。"
```

---

## Task 6: 全量校验与三角色浏览器验证

**Files:** 无(校验任务)

- [ ] **Step 1: 前端全量测试 + 构建 + lint**

Run:
```bash
cd web && npx vitest run && npx vue-tsc --noEmit && npm run build
```
Expected: 全部 PASS、类型检查无错、build 成功。若仓库有前端 lint 脚本(查 `web/package.json` 的 `lint`),一并跑过。

- [ ] **Step 2: 部署到本地 k3d**

按 [[local-k3d-env]] / 项目本地流程重建 manager 前端镜像并 rollout(本功能无后端/迁移改动,仅前端)。确认 `manager-api` Pod 就绪、前端资源已更新(浏览器硬刷新)。

- [ ] **Step 3: 三角色浏览器验证(真实浏览器,逐项留证)**

**org_admin(无自有实例)**:
1. 登录后默认「企业管理视角」,菜单为 总览/成员/实例/企业知识库/账户余额/审计/用量,**无独立「技能」**。
2. 顶栏点「我的实例」→ 导航到 `/apps/empty`,菜单切为成员同款(总览/渠道/工作目录/个人知识库/企业知识库/技能/任务/定时任务/用量);空状态页显示「去成员页创建实例」按钮。
3. 点该按钮 → 跳 `/members`;在自己那一行用「为该成员创建实例」建实例成功。
4. 回「我的实例」视角,各实例页(总览/渠道/...)正常驱动到自有实例;刷新页面仍停留在「我的实例」视角(localStorage 持久化)。
5. 无实例阶段:「我的实例 › 技能」仍能打开并提交定制技能工单(`OrgSkillsPage` 工单面板)。

**org_admin(有自有实例)**:
6. 「我的实例 › 技能 › 技能市场」筛选「定制」,安装一个「仅管理员可见」的定制技能到自有实例并生效(容器内 `data/skills/<name>` 存在 + 已安装 tab 显示已生效)。
7. 顶栏切回「企业管理视角」→ 管理菜单正常,实例列表、成员、审计等可用。

**回归(org_member 与 platform_admin)**:
8. org_member 登录:**无视角切换器**;菜单与现状一致(总览/渠道/工作目录/个人知识库/企业知识库/技能/任务/定时任务/用量),实例与技能正常。
9. platform_admin 登录:**无视角切换器**;控制台菜单与现状一致。

- [ ] **Step 4: 交付前检查**

确认:工作区无无关改动;OpenAPI / 生成类型无需变更(无后端改动,跑 `make openapi-check` 应干净);逐项验证留证;若验证中发现问题,先修复并重新验证直至通过。

---

## Self-Review(计划自查)

- **Spec 覆盖**:① 顶栏切换器仅 admin → Task 4;② 企业管理菜单(去技能)/我的实例菜单(成员同款) → Task 3;③ 默认 manage + localStorage 持久化 + 退出清除 → Task 1(持久化)+Task 4(退出清除);④ 无实例跳成员页复用建实例 → Task 5;⑤ 技能页无实例仍可提交工单 → 复用现有 `OrgSkillsPage`(Task 6 Step 3.5 验证,无需改代码);⑥ 后端零改动 → 全程未涉及后端;⑦ 测试(菜单分支/切换器可见性/localStorage/三角色浏览器)→ Task 1/3/4/5 单测 + Task 6 浏览器。无遗漏。
- **占位符扫描**:无 TBD/TODO;每个代码步骤含完整代码与确切命令。
- **类型/命名一致**:`AdminPerspective`('manage'|'instance')贯穿 Task 1/3/4;composable 返回 `{ perspective, setPerspective, resetPerspective }` 在 Task 4 全部使用(Task 3 仅用 `perspective`,Task 4 扩展解构,避免未用变量);`memberAppId/memberHasApp` 在 Task 2 引入、Task 4 的 `onSwitchPerspective` 复用;localStorage 键 `oc.admin.perspective` 在实现与测试一致。
