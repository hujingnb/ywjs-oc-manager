# 定制技能 Plan 3：前端与端到端验证 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现「定制技能」三处前端界面（用户侧「技能」页第三个 tab「定制技能」工单系统、技能市场「定制」筛选卡片、平台管理员后台「定制技能」工单队列+交付弹窗+待处理角标），并完成真实浏览器三角色端到端验证。

**Architecture:** Vue3 + Pinia + Naive UI + TanStack Query。新建 `useSkillTickets.ts`（工单/附件/交付 hooks）；`SkillTicketPanel.vue`（成员工单面板，挂进 `SkillManager.vue` 第三 tab，由 `showTickets` prop 门控，仅成员 `/skills` 启用）；扩展 `SkillMarketBrowser.vue` 加 `custom` 筛选与定制卡片；新建平台后台页 `CustomSkillTicketsPage.vue`（队列+宽抽屉+交付弹窗，交付复用 `skillPackaging.ts` 的打包）。菜单角标用 `useSkillTicketBadgeQuery`。

**Tech Stack:** Vue3 / Vite / Pinia / Naive UI / TanStack Query / vitest / vue-tsc。

「定制技能」设计（`docs/superpowers/specs/2026-06-10-定制技能-design.md`）Plan 3（共 3 个，最后一个）。**依赖 Plan 1/2 后端已合入并跑过 `make openapi-gen`+`make web-types-gen`**，使 `web/src/api/generated.ts` 含 `/skill-tickets*`、`/admin/skill-tickets*`、`/custom-skills/deliver`、`/skill-tickets/{id}/attachments*` 路由与 `service.SkillTicketResult`/`SkillTicketDetailResult`/`SkillTicketCommentResult`/`CustomSkillResult`/`SkillTicketAttachmentResult` 类型、`SkillEntry` 的 `requester_name`/`audience` 字段。

> **现场确认项**：① `generated.ts` 实际字段名（hook body/字段按生成结果对齐）；② `useSkills.ts`、`SkillManager.vue`、`SkillMarketBrowser.vue`、`OrgSkillsPage.vue`、`DashboardLayout.vue`、`router.ts`、`skillPackaging.ts`、`api/client.ts`、`stores/auth.ts`、`api/index.ts` 的实际签名与行号；③ Naive UI 菜单项挂角标的实际方式。校验：`cd web && npm run typecheck`（vue-tsc）+ `npm test`（vitest）。

---

## File Structure

新建：
- `web/src/api/hooks/useSkillTickets.ts`（+ `useSkillTickets.spec.ts`）— 工单/附件/交付 hooks
- `web/src/components/SkillTicketPanel.vue`（+ `SkillTicketPanel.spec.ts`）— 成员工单面板（列表+提交+抽屉对话+附件）
- `web/src/pages/platform/CustomSkillTicketsPage.vue`（+ `CustomSkillTicketsPage.spec.ts`）— 平台后台工单队列+交付弹窗

修改：
- `web/src/api/index.ts` — 加类型 alias
- `web/src/components/SkillManager.vue` — 加第三 tab「定制技能」（`showTickets` prop 门控）
- `web/src/pages/skills/OrgSkillsPage.vue` — 传 `:show-tickets="true"`
- `web/src/components/SkillMarketBrowser.vue` — 加 `custom` 筛选 + 定制卡片（申请人+范围徽章）
- `web/src/app/router.ts` — 加 `/platform/custom-skills` 平台路由
- `web/src/layouts/DashboardLayout.vue` — 平台菜单加「定制技能」项 + 待处理角标 + activeKey 前缀

---

## Task 1：类型 alias + useSkillTickets hooks

**Files:** Modify `web/src/api/index.ts`；Create `web/src/api/hooks/useSkillTickets.ts`、`useSkillTickets.spec.ts`

- [ ] **Step 1: 类型 alias**

在 `web/src/api/index.ts` 加（字段按 `generated.ts` 实际收紧，先 `grep -n 'SkillTicketResult\|SkillTicketDetailResult\|SkillTicketCommentResult\|CustomSkillResult\|SkillTicketAttachmentResult' web/src/api/generated.ts` 确认）：

```ts
export type SkillTicket = WithRequired<Schemas['service.SkillTicketResult'], 'id' | 'status' | 'title'>
export type SkillTicketDetail = WithRequired<Schemas['service.SkillTicketDetailResult'], 'id' | 'status'>
export type SkillTicketComment = WithRequired<Schemas['service.SkillTicketCommentResult'], 'id' | 'body'>
export type CustomSkillResult = WithRequired<Schemas['service.CustomSkillResult'], 'id' | 'name' | 'version'>
export type SkillTicketAttachment = WithRequired<Schemas['service.SkillTicketAttachmentResult'], 'id' | 'file_name'>
```

- [ ] **Step 2: 写 hooks（抄 `useSkills.ts` 的 query/mutation 骨架 + `apiRequest`/`xhrUpload`）**

Create `web/src/api/hooks/useSkillTickets.ts`：

```ts
import { computed, type Ref } from 'vue'
import { useQuery, useMutation, useQueryClient } from '@tanstack/vue-query'

import { apiRequest } from '@/api/client'
import { xhrUpload } from '@/api/xhrUpload'
import type { SkillTicket, SkillTicketDetail, SkillTicketComment, SkillTicketAttachment, CustomSkillResult } from '@/api'

// ===== queryKeys =====
const mineKey = () => ['skill-tickets', 'mine'] as const
const adminKey = () => ['skill-tickets', 'admin'] as const
const badgeKey = () => ['skill-tickets', 'badge'] as const
const detailKey = (id: string | undefined) => ['skill-tickets', 'detail', id] as const
const attachKey = (id: string | undefined) => ['skill-tickets', 'attachments', id] as const

// ===== 查询 =====
export function useMySkillTicketsQuery() {
  return useQuery<SkillTicket[]>({
    queryKey: mineKey(),
    queryFn: async () => (await apiRequest<{ tickets: SkillTicket[] }>('/api/v1/skill-tickets')).tickets ?? [],
  })
}

export function useAdminSkillTicketsQuery() {
  return useQuery<SkillTicket[]>({
    queryKey: adminKey(),
    queryFn: async () => (await apiRequest<{ tickets: SkillTicket[] }>('/api/v1/admin/skill-tickets')).tickets ?? [],
  })
}

export function useSkillTicketBadgeQuery() {
  return useQuery<number>({
    queryKey: badgeKey(),
    refetchInterval: 60_000, // 后台菜单角标每分钟刷新
    queryFn: async () => (await apiRequest<{ pending: number }>('/api/v1/admin/skill-tickets/badge')).pending ?? 0,
  })
}

export function useSkillTicketDetailQuery(id: Ref<string | undefined>) {
  return useQuery<SkillTicketDetail>({
    queryKey: computed(() => detailKey(id.value)),
    enabled: () => Boolean(id.value),
    queryFn: async () => (await apiRequest<{ ticket: SkillTicketDetail }>(`/api/v1/skill-tickets/${id.value}`)).ticket,
  })
}

export function useSkillTicketAttachmentsQuery(id: Ref<string | undefined>) {
  return useQuery<SkillTicketAttachment[]>({
    queryKey: computed(() => attachKey(id.value)),
    enabled: () => Boolean(id.value),
    queryFn: async () => (await apiRequest<{ attachments: SkillTicketAttachment[] }>(`/api/v1/skill-tickets/${id.value}/attachments`)).attachments ?? [],
  })
}

// ===== 提交 / 评论（提交者）=====
export function useSubmitSkillTicket() {
  const client = useQueryClient()
  return useMutation({
    mutationFn: async (input: { title: string; description: string }) =>
      (await apiRequest<{ ticket: SkillTicket }>('/api/v1/skill-tickets', { method: 'POST', body: input })).ticket,
    onSuccess: () => void client.invalidateQueries({ queryKey: mineKey() }),
  })
}

export function useAddSkillTicketComment(id: Ref<string | undefined>) {
  const client = useQueryClient()
  return useMutation({
    mutationFn: async (input: { body: string }) =>
      (await apiRequest<{ comment: SkillTicketComment }>(`/api/v1/skill-tickets/${id.value}/comments`, { method: 'POST', body: input })).comment,
    onSuccess: () => {
      // 评论可能触发重开 → 刷新详情 + 我的列表 + 管理员列表 + 角标
      void client.invalidateQueries({ queryKey: detailKey(id.value) })
      void client.invalidateQueries({ queryKey: mineKey() })
      void client.invalidateQueries({ queryKey: adminKey() })
      void client.invalidateQueries({ queryKey: badgeKey() })
    },
  })
}

// ===== 附件（提交者/管理员）=====
export function useUploadSkillTicketAttachment(id: Ref<string | undefined>) {
  const client = useQueryClient()
  return useMutation({
    mutationFn: async (file: File) => {
      const form = new FormData()
      form.append('file', file)
      const resp = await xhrUpload(`/api/v1/skill-tickets/${id.value}/attachments`, { method: 'POST', body: form })
      return (resp.body as { attachment: SkillTicketAttachment }).attachment
    },
    onSuccess: () => void client.invalidateQueries({ queryKey: attachKey(id.value) }),
  })
}

// downloadSkillTicketAttachment 带鉴权下载:fetch 取 blob 再触发浏览器保存(下载端点需 Bearer,不能用裸 <a href>)。
export async function downloadSkillTicketAttachment(ticketId: string, att: SkillTicketAttachment) {
  const blob = await apiRequest<Blob>(`/api/v1/skill-tickets/${ticketId}/attachments/${att.id}/download`, { responseType: 'blob' } as never)
  const url = URL.createObjectURL(blob)
  const a = document.createElement('a')
  a.href = url
  a.download = att.file_name
  a.click()
  URL.revokeObjectURL(url)
}

// ===== 管理员处理 =====
export function useUpdateSkillTicketStatus() {
  const client = useQueryClient()
  return useMutation({
    mutationFn: async (input: { id: string; status: string }) =>
      apiRequest<void>(`/api/v1/skill-tickets/${input.id}/status`, { method: 'PATCH', body: { status: input.status } }),
    onSuccess: (_d, v) => invalidateAdmin(client, v.id),
  })
}

export function useSetSkillTicketQuote() {
  const client = useQueryClient()
  return useMutation({
    mutationFn: async (input: { id: string; quote_amount_cents: number }) =>
      apiRequest<void>(`/api/v1/skill-tickets/${input.id}/quote`, { method: 'PATCH', body: { quote_amount_cents: input.quote_amount_cents } }),
    onSuccess: (_d, v) => invalidateAdmin(client, v.id),
  })
}

export function useRejectSkillTicket() {
  const client = useQueryClient()
  return useMutation({
    mutationFn: async (input: { id: string; reason: string }) =>
      apiRequest<void>(`/api/v1/skill-tickets/${input.id}/reject`, { method: 'POST', body: { reason: input.reason } }),
    onSuccess: (_d, v) => invalidateAdmin(client, v.id),
  })
}

// ===== 交付（管理员，multipart：ticket_id/description/targets(JSON)/file）=====
export function useDeliverCustomSkill() {
  const client = useQueryClient()
  return useMutation({
    mutationFn: async (input: { ticketId: string; description: string; targets: Array<{ org_id: string; audience: string }>; file: File }) => {
      const form = new FormData()
      form.append('ticket_id', input.ticketId)
      form.append('description', input.description)
      form.append('targets', JSON.stringify(input.targets))
      form.append('file', input.file)
      const resp = await xhrUpload('/api/v1/custom-skills/deliver', { method: 'POST', body: form })
      return (resp.body as { skill: CustomSkillResult }).skill
    },
    onSuccess: (_d, v) => {
      invalidateAdmin(client, v.ticketId)
      void client.invalidateQueries({ queryKey: ['skills', 'market'] }) // 市场定制筛选刷新
    },
  })
}

function invalidateAdmin(client: ReturnType<typeof useQueryClient>, id: string) {
  void client.invalidateQueries({ queryKey: detailKey(id) })
  void client.invalidateQueries({ queryKey: adminKey() })
  void client.invalidateQueries({ queryKey: badgeKey() })
}
```

> `apiRequest` 的 blob 下载分支：若现有 `apiRequest` 不支持 `responseType: 'blob'`，改用 `xhrUpload`/原生 `fetch`（带 `Authorization`）取 blob——执行前看 `api/client.ts` 是否已有 blob 能力（knowledge 下载可能已有同类 helper，优先复用）。

- [ ] **Step 3: 写 spec + 校验**

`useSkillTickets.spec.ts`（vitest，抄 `useSkills.spec.ts`）：断言各 query 的 queryKey 与 queryFn 调对 path；submit/comment/deliver mutation 的 onSuccess invalidate 正确 key。

Run: `cd web && npm run typecheck && npm test -- useSkillTickets` → 通过。提交：`feat(web): 增加定制技能工单/附件/交付 hooks 与类型 alias`。

---

## Task 2：成员工单面板 SkillTicketPanel + 挂进 SkillManager 第三 tab

**Files:** Create `web/src/components/SkillTicketPanel.vue`、`SkillTicketPanel.spec.ts`；Modify `web/src/components/SkillManager.vue`、`web/src/pages/skills/OrgSkillsPage.vue`

- [ ] **Step 1: SkillTicketPanel 组件**

Create `web/src/components/SkillTicketPanel.vue`（自包含，无需 appId；emits `goInstall` 让父组件跳市场定制筛选）。结构：

- **工单列表**：`useMySkillTicketsQuery` → `n-data-table`，列：标题、状态徽章（`statusTag(status)`：pending 默认/processing 警告/delivered 成功/rejected 错误）、报价（`quote_amount_cents != null ? ¥${(c/100).toFixed(2)} : '—'`）、更新时间；行尾「查看」按钮打开抽屉；`delivered` 行额外「去安装」按钮 → `emit('goInstall', row.custom_skill_name)`。
- **「+ 提交需求」**：`n-button` 打开提交 `n-modal`（表单：标题 `n-input`、描述 `n-input textarea`、可选附件——先建工单再逐个上传附件，或建后在抽屉里补传；首版：提交仅标题+描述，附件在详情抽屉补传）→ `useSubmitSkillTicket`，成功 `message.success` + 关闭。
- **详情抽屉**（`n-drawer` placement=right width≈480，仿 `SkillDetailDrawer.vue`）：`useSkillTicketDetailQuery(selectedId)` →
  - 头部：标题 + 状态徽章 + 报价；`rejected` 时显示 `reject_reason` + 提示「补充说明后将重新提交」。
  - 对话流：`detail.comments` 气泡（按 `author_user_id` 是否本人左右区分）。
  - 附件区：`useSkillTicketAttachmentsQuery(selectedId)` 列表 + 每条「下载」(`downloadSkillTicketAttachment`)；上传 `useUploadSkillTicketAttachment`（原生 file input）。
  - 回复框：`n-input` + 发送 → `useAddSkillTicketComment`（**任何状态可发**；delivered/rejected 发送即重开，hook 已 invalidate → 状态自动刷新）。
  - `delivered` 抽屉显「去安装」→ `emit('goInstall', detail.custom_skill_name)`。

关键脚本骨架：

```ts
const message = useMessage()
const selectedId = ref<string | undefined>()
const showSubmit = ref(false)
const { data: tickets, isLoading } = useMySkillTicketsQuery()
const detailQuery = useSkillTicketDetailQuery(selectedId)
const submitMut = useSubmitSkillTicket()
const commentMut = useAddSkillTicketComment(selectedId)
const uploadMut = useUploadSkillTicketAttachment(selectedId)
const emit = defineEmits<{ goInstall: [name: string | undefined] }>()

function statusTag(s: string) {
  return ({ pending: { type: 'default', label: '待处理' }, processing: { type: 'warning', label: '制作中' },
    delivered: { type: 'success', label: '已交付' }, rejected: { type: 'error', label: '已拒绝' } } as const)[s]
    ?? { type: 'default', label: s }
}
function yuan(cents?: number | null) { return cents == null ? '—' : `¥${(cents / 100).toFixed(2)}` }
```

- [ ] **Step 2: SkillManager 加第三 tab**

`web/src/components/SkillManager.vue`：`defineProps` 加 `showTickets?: boolean`（默认 false）。在内部 `<n-tabs>` 末尾加：

```vue
<n-tab-pane v-if="showTickets" name="tickets" tab="定制技能">
  <SkillTicketPanel @go-install="onGoInstall" />
</n-tab-pane>
```

`onGoInstall(name)`：把内部 `activeTab.value = 'market'`、市场来源 `marketSource.value = 'custom'`，并（可选）记录高亮 name 传给 `SkillMarketBrowser`。市场 tab 的来源 selected 通过 v-model 或 prop 传给 `SkillMarketBrowser`（Task 3 暴露 `:source` prop 或 `defineExpose` setSource）。

- [ ] **Step 3: OrgSkillsPage 开启 tickets**

`web/src/pages/skills/OrgSkillsPage.vue`：给 `<SkillManager>` 传 `:show-tickets="true"`（管理员 per-app 的 `AppSkillsTab` 不传，保持 false，后台不显示成员工单 tab）。

- [ ] **Step 4: spec + 校验**

`SkillTicketPanel.spec.ts`（vitest）：mock hooks，stub Naive；断言四类状态徽章、报价格式化、delivered 行有「去安装」并 emit、提交按钮打开 modal、rejected 抽屉显示拒绝原因。

Run: `cd web && npm run typecheck && npm test -- SkillTicketPanel` → 通过。提交：`feat(web): 成员定制技能工单面板与 SkillManager 第三 tab`。

---

## Task 3：技能市场「定制」筛选 + 定制卡片

**Files:** Modify `web/src/components/SkillMarketBrowser.vue`

- [ ] **Step 1: 加 custom 筛选**

`SkillMarketBrowser.vue` 的 `sourceFilters` 加一项：

```ts
const sourceFilters = [
  { label: '全部', value: '' },
  { label: '平台技能', value: 'platform' },
  { label: 'ClawHub', value: 'clawhub' },
  { label: '定制', value: 'custom' }, // 新增:定向定制技能
] as const
```

> 若 `source` 由父组件经 prop 传入（SkillManager 的 `onGoInstall` 要能设成 `custom`），把 `selectedSource` 做成 `v-model`/prop 受控，或 `defineExpose({ setSource })`，使「去安装」能切到 custom 筛选。

- [ ] **Step 2: 定制卡片渲染**

市场卡片在 `entry.source === 'custom'` 时额外渲染：
- 范围徽章：`audienceTag(entry.audience)` —— `all_org`→「整企业可见」(success)、`org_admins`→「仅管理员可见」(warning)、`requester_only`→「仅本人可见」(default)。
- 申请人：`entry.requester_name` → 「由 {name} 申请」小字。
- 来源徽章 `sourceLabel('custom')`→「定制」、`sourceTagType('custom')`→ 紫色（`'error'`/自定义 class，按现有 `sourceTagType` 风格加 case）。

模板片段（在现有卡片 footer/header 区按 source 条件插入）：

```vue
<template v-if="entry.source === 'custom'">
  <n-tag size="small" :type="audienceTag(entry.audience).type">{{ audienceTag(entry.audience).label }}</n-tag>
  <span v-if="entry.requester_name" class="market-card-requester">由 {{ entry.requester_name }} 申请</span>
</template>
```

```ts
function audienceTag(a?: string) {
  return ({ all_org: { type: 'success', label: '整企业可见' }, org_admins: { type: 'warning', label: '仅管理员可见' },
    requester_only: { type: 'default', label: '仅本人可见' } } as const)[a ?? '']
    ?? { type: 'default', label: '' }
}
```

安装按钮复用现有逻辑（`emitAction(entry, version)` → 父级 `useInstallAppSkill`，source=`custom`、source_ref=`entry.source_ref`(=name)、name=`entry.name`、version=`entry.version`）。

- [ ] **Step 3: 校验**

Run: `cd web && npm run typecheck && npm test -- SkillMarketBrowser`（若有现有 spec，补一条 custom 卡片断言；无则加最小 spec）。提交：`feat(web): 技能市场加定制筛选与定制卡片(范围徽章+申请人)`。

---

## Task 4：平台后台「定制技能」工单队列 + 交付弹窗 + 路由/菜单/角标

**Files:** Create `web/src/pages/platform/CustomSkillTicketsPage.vue`、`CustomSkillTicketsPage.spec.ts`；Modify `web/src/app/router.ts`、`web/src/layouts/DashboardLayout.vue`

- [ ] **Step 1: 后台页组件（仿 `PlatformSkillsPage.vue` 骨架）**

Create `web/src/pages/platform/CustomSkillTicketsPage.vue`：

- **工单队列**（`n-card`「定制技能工单」）：`useAdminSkillTicketsQuery` → `n-data-table`，列：标题、提交者（`requester_user_id` + `requester_role` 显示「成员/管理员」；org 可加列）、状态徽章、报价、更新时间、操作「处理」按钮打开抽屉。顶部筛选：状态 `n-select`（全部/pending/processing/delivered/rejected）+ 关键字，前端 `computed` 过滤（数据量小）。
- **宽抽屉**（`n-drawer` width≈640）：左需求（标题/描述/附件下载）+ 对话流 + 回复框（`useAddSkillTicketComment`）；右操作区：
  - 状态 `n-select`（pending/processing）→ `useUpdateSkillTicketStatus`。
  - 报价 `n-input-number`（元，提交时 `*100` 取整成分）→ `useSetSkillTicketQuote`。
  - 「拒绝」按钮 → `n-modal` 填原因 → `useRejectSkillTicket`。
  - 「交付」按钮 → 打开**交付弹窗**。
- **交付弹窗**（`n-modal`，复用 `skillPackaging.ts`）：
  - 上传方式 `n-radio-group`（粘贴 Markdown / 上传文件夹）—— **整段交互照抄 `PlatformSkillsPage.vue`**（`mode`/`mdText`/`folderFiles`/`onFolderChange`/`triggerFolderInput`/`parsed`），打包用 `packFromMarkdown`/`packFromFolder` 得 `{ name, description, tar }`。
  - 描述 `n-input`（默认取自解析的 description，可改）；**无版本输入**（后端按上传时间自动生成）。
  - **目标范围**编辑器：默认据工单 `requester_role` + `org_id` 预填一条（member→`all_org`、admin→`org_admins`）；可改受众 `n-select`（all_org/org_admins/requester_only）、可「+ 加组织」（`n-select` 选 org → 追加一条 `{org_id, audience}`）。
  - 「确认交付」→ `useDeliverCustomSkill({ ticketId, description, targets, file: new File([tar], `${name}.tar`, {type:'application/x-tar'}) })`；成功 `message.success` + 关抽屉（hook 已 invalidate 列表/角标/市场）。改名冲突时后端回 409，`message.error` 展示「迭代必须沿用同一技能名」。

默认目标范围推导：

```ts
function defaultTargets(ticket: SkillTicketDetail): Array<{ org_id: string; audience: string }> {
  const audience = ticket.requester_role === 'org_admin' ? 'org_admins' : 'all_org'
  return [{ org_id: ticket.org_id ?? '', audience }]
}
```

- [ ] **Step 2: 路由**

`web/src/app/router.ts` 加（紧邻 `platform/skills`，`PLATFORM_ONLY`）：

```ts
{ path: 'platform/custom-skills', component: CustomSkillTicketsPage, meta: { allowedRoles: PLATFORM_ONLY } },
```

并 import 组件。

- [ ] **Step 3: 菜单 + 角标**

`web/src/layouts/DashboardLayout.vue` 平台管理员分支 menuOptions，在「平台技能」后加「定制技能」项（key `/platform/custom-skills`，lucide 图标如 `Wrench` 或 `Sparkles`）。角标：用 `useSkillTicketBadgeQuery` 取 pending 数，菜单 label 渲染成带数字（Naive 菜单项 label 支持 render 函数，渲染 `label + (count>0 ? 角标 : '')`，或在 label 后拼 `n-badge`）：

```ts
const badge = useSkillTicketBadgeQuery()
// menuOption:
{ key: '/platform/custom-skills',
  label: () => h('span', [ '定制技能', badge.data.value ? h(NBadge, { value: badge.data.value, style: 'margin-left:8px' }) : null ]),
  icon: () => h(Wrench, { size: 18 }) }
```

`activeKey` 前缀数组加 `'/platform/custom-skills'`。

- [ ] **Step 4: spec + 校验**

`CustomSkillTicketsPage.spec.ts`（vitest）：mock hooks，stub Naive；断言队列渲染、状态筛选、抽屉操作按钮存在、交付弹窗默认目标范围推导（member→all_org、admin→org_admins）、交付提交构造的 targets/file 正确。

Run: `cd web && npm run typecheck && npm test -- CustomSkillTickets` → 通过。提交：`feat(web): 平台后台定制技能工单队列、交付弹窗与待处理角标`。

---

## Task 5：整体校验 + 真实浏览器三角色端到端验证

> CLAUDE.md 硬性要求：**真实浏览器**（非 curl/API）。本地 k3d + 账号见 AGENTS.md（平台管理员 admin/admin123；org 账号见 [[verification-rigor]] 记忆：`e2e-org-admin`/`e2e-org-member`，密码 `e2e-pass-123`，组织标识 `test-org`）。**遇微信扫码登录渠道 / 微信真实发消息触发对话等需人工配合的步骤，暂停并通知用户来操作，不跳过、不用 curl 替代。**

- [ ] **Step 1: 前端全量** — `cd web && npm run typecheck && npm test`，全过。
- [ ] **Step 2: 构建** — `cd web && npm run build`（vite），无错误。
- [ ] **Step 3: 起本地环境** — 确认 k3d 在跑、000010 迁移已应用、manager 重新部署含 Plan1/2 后端（`make local-up` 或对应 rollout）。若 br_netfilter 致 CrashLoop 见 [[project-local-k3d-br-netfilter]]。
- [ ] **Step 4: 浏览器三角色端到端**（发现问题先修再验，fix-reverify 闭环直到全部正常）：
  - **企业成员**（`e2e-org-member`）：
    1. 技能页 →「定制技能」tab → 提交需求（标题+描述）→ 列表出现 pending。
    2. 打开抽屉 → 传一个附件 → 列表/抽屉可见、可下载。
    3.（管理员交付后，见下）市场「定制」筛选看到卡片（范围徽章「整企业可见」+「由 X 申请」）→ 安装 → 新对话验证技能真实生效（**需微信发消息，通知用户配合**；坐实容器 `data/skills/<name>/` 存在 + 对话命中，参照 [[verification-rigor]]）。
    4. 管理员拒绝后 → 抽屉补评论 → 工单回 pending（重开）。
    5. 再次交付新版本后 → 「已安装」看到「更新」→ 点更新生效。
  - **企业管理员**（`e2e-org-admin`）：
    1. 提交需求 → 交付后验证「仅管理员可见」：该 org 普通成员市场定制筛选**看不到**此卡片、管理员**能看到**。
    2. 成员提交并交付（all_org）的定制技能，该管理员在市场定制筛选**也能看到**（member→整企业含管理员）。
  - **平台管理员**（admin/admin123）：
    1. 左侧「定制技能」菜单待处理角标数 = pending 工单数；新工单/重开后角标 +1，接单后 −1。
    2. 工单队列筛选（状态/关键字）、打开宽抽屉、改状态/报价/回复/拒绝。
    3. 交付弹窗：粘贴 Markdown 与上传文件夹两种方式各交付一次；目标范围默认（member 工单→整企业、admin 工单→仅管理员）可改、可加组织；确认交付 → 工单转 delivered、市场对目标用户可见。
    4. 对同一工单**再次交付**：改成不同技能名 → 期望 409「迭代必须沿用同一技能名」；同名 → 成功加版本。
- [ ] **Step 5: 逐项验证矩阵** — 交付时给「角色 × 场景 → 验证方式 → 证据」矩阵（[[verification-rigor]] 要求），不要只说「都验证过了」。
- [ ] **Step 6: 提交**（若浏览器验证修了 bug，按业务边界拆分提交）。

---

## Self-Review 备注（写计划时已核对）

- **Spec 覆盖**：用户侧「技能」页第三 tab「定制技能」（工单列表/提交/抽屉对话/附件/去安装）；市场「定制」筛选（范围徽章 + 申请人 + 安装）；平台后台工单队列 + 宽抽屉 + 交付弹窗（复用打包、版本不填、目标范围默认+可调）+ 待处理角标；版本迭代「更新」复用现有机制；拒绝可重开；三角色真实浏览器验证含微信配合点。
- **复用**：交付弹窗复用 `skillPackaging.ts`（`packFromMarkdown`/`packFromFolder`）与 `PlatformSkillsPage.vue` 的文件夹/MD 交互；成员工单面板挂进 `SkillManager` 第三 tab（`showTickets` 门控，避免污染管理员 per-app 视图）；市场卡片复用 `SkillMarketBrowser`。
- **跨 tab 跳转**：工单「去安装」→ `SkillManager` 切市场 tab + 来源设 `custom`（需 `SkillMarketBrowser` 的 source 受控/expose）。
- **依赖**：本 plan 全部依赖 Plan 1/2 后端合入 + openapi/web-types 同步后 `generated.ts` 才有类型与路由；`SkillEntry.requester_name/audience` 来自 Plan 2 Task 4。
- **待执行现场确认项**：① `generated.ts` 实际字段与必填项（alias 收紧）；② `apiRequest` 是否支持 blob 下载（否则用 fetch+Bearer）；③ `SkillManager`/`SkillMarketBrowser` 的 source 受控方式；④ Naive 菜单项 label render + 角标的实际写法；⑤ `xhrUpload` 签名；⑥ `auth.user` 的 username/display_name 字段名。
- **验证**：vitest 单测 + 真实浏览器三角色（不可 curl 替代，CLAUDE.md 硬性），微信相关步骤通知用户配合。
```
