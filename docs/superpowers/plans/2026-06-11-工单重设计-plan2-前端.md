# 定制技能工单重设计 · Plan 2 前端 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> **依赖 Plan 1 后端已合入**(新端点 + 契约 generated.ts 已更新)。

**Goal:** 前端把工单详情改为双侧共享的 role-aware 整页(状态步骤条 + 情境化动作 + 统一消息对话 + 可见范围编辑),列表 keep-alive 保活;平台队列与需求方面板瘦身为「列表→整页」。

**Architecture:** 新增共享路由 `/skill-tickets/:id`(`TicketDetailPage`,按角色渲染)+ 四个共享组件(状态步骤条 / 对话 / 可见范围编辑器 / 交付弹窗)。hooks 重写为消息+状态动作+targets。`DashboardLayout` 的 `<RouterView>` 包 `<keep-alive>` 保活两个列表页。

**Tech Stack:** Vue 3 script setup + TS + Pinia + Naive UI + TanStack Query + Vitest;上传走 `xhrUpload`,下载走 `apiDownload`。

---

## 背景事实(实现前必读)

- 设计文档:`docs/superpowers/specs/2026-06-11-定制技能工单重设计-design.md`。Plan 1 已提供新端点。
- **新后端端点**(Plan 1):`GET /skill-tickets/:id`(详情含 `messages`)、`POST /:id/messages`(text)、`POST /:id/messages/upload`(multipart file)、`GET /:id/messages/:msgId/download`、`POST /:id/start`、`POST /:id/reopen`、`PATCH /:id/quote`、`POST /:id/reject`、`PATCH /:id/targets`、`POST /custom-skills/deliver`、`GET /admin/skill-tickets`、`GET /admin/skill-tickets/badge`、`GET /skill-tickets`(我的)。
- **消息类型**(generated):`service.SkillTicketMessageResult { id, author_user_id, kind('text'|'image'|'file'), text?, file_name?, file_size?, content_type?, created_at }`;`SkillTicketDetailResult.messages`。`SkillTicketComment*`/`Attachment*` 已删。
- **现状文件**:hooks `web/src/api/hooks/useSkillTickets.ts`;平台页 `web/src/pages/platform/CustomSkillTicketsPage.vue`(队列+抽屉+交付弹窗,含 targets 编辑器 + skillPackaging 打包逻辑);需求方 `web/src/components/SkillTicketPanel.vue`(列表+提交+抽屉);挂载 `web/src/components/SkillManager.vue`(第三 tab,`@go-install`);路由 `web/src/app/router.ts`(`platform/custom-skills`=PLATFORM_ONLY、`skills`=无限制);`web/src/layouts/DashboardLayout.vue:100-103` 的 `<RouterView>` 无 keep-alive。
- **基建**:`xhrUpload(url,{method,body:FormData})`、`apiDownload(path)→{blob,filename}`、`apiRequest`。打包逻辑 `web/src/domain/skillPackaging.ts`(`packFromMarkdown/packFromFolder/parseSkillFrontmatter`)。组织列表 `useOrganizationsQuery`。
- **测试范式**:`web/src/components/SkillTicketPanel.spec.ts`(`vi.hoisted` + `vi.mock('naive-ui'/'@/api/hooks/useSkillTickets'/'@/stores/auth')` + 内联表格 stub)。
- **命令**:`cd web && npx vitest run <file>`;`npx vue-tsc --noEmit`;`npm run build`。

---

## File Structure

- **Modify** `web/src/api/hooks/useSkillTickets.ts` — 删评论/附件/通用状态 hooks;加消息/状态动作/targets hooks。
- **Create** `web/src/pages/skill-tickets/TicketDetailPage.vue` — 共享整页详情(role-aware)。
- **Create** `web/src/components/ticket/TicketStatusStepper.vue` — 状态步骤条。
- **Create** `web/src/components/ticket/TicketConversation.vue` — 消息流 + composer(text/image/file)。
- **Create** `web/src/components/ticket/TicketTargetsEditor.vue` — 可见范围编辑器(交付弹窗与「编辑可见范围」共用)。
- **Create** `web/src/components/ticket/DeliverCustomSkillModal.vue` — 交付弹窗(抽自现 CustomSkillTicketsPage,复用 skillPackaging + TicketTargetsEditor)。
- **Modify** `web/src/app/router.ts` — 加 `/skill-tickets/:id` 路由。
- **Modify** `web/src/layouts/DashboardLayout.vue` — `<RouterView>` 包 `<keep-alive>`。
- **Modify** `web/src/pages/platform/CustomSkillTicketsPage.vue` — 瘦身为队列;`defineOptions({name})`;行跳整页。
- **Modify** `web/src/components/SkillTicketPanel.vue` — 瘦身为列表+提交;行跳整页;提交可附文件→落消息。
- **Modify** `web/src/pages/skills/OrgSkillsPage.vue` — `defineOptions({name})`(keep-alive include 用)。

---

## Task 1: hooks + 类型重写

**Files:**
- Modify: `web/src/api/hooks/useSkillTickets.ts`
- Test: `web/src/api/hooks/useSkillTickets.spec.ts`(若有,更新;否则新建针对失效逻辑的最小测试)

- [ ] **Step 1: 写/改失败测试**(覆盖关键 invalidate)

针对新 hooks 写失效断言(参照 `useMembers.spec.ts`/既有 hook 测试范式,真实 QueryClient + spy invalidateQueries):

```ts
// 发消息成功 → 失效 detail + admin + mine + badge(消息可能触发重开,需多视图刷新)。
it('useSendTicketMessage invalidates detail/admin/mine/badge', async () => {})
// 开始制作成功 → 失效 detail + admin + badge。
it('useStartTicket invalidates detail/admin/badge', async () => {})
// 更新 targets 成功 → 失效 detail + 市场缓存 ['skills','market']。
it('useUpdateTicketTargets invalidates detail + market', async () => {})
```

- [ ] **Step 2: 跑确认失败**

Run: `cd web && npx vitest run src/api/hooks/useSkillTickets.spec.ts` → 失败(hook 不存在)。

- [ ] **Step 3: 改实现**

`web/src/api/hooks/useSkillTickets.ts`:

(a) **保留**:`useMySkillTicketsQuery`、`useAdminSkillTicketsQuery`、`useSkillTicketBadgeQuery`、`useSubmitSkillTicket`、`useSetSkillTicketQuote`、`useRejectSkillTicket`、`useDeliverCustomSkill`、`DeliverTarget`、各 key 函数与 `invalidateAdmin`。`useSkillTicketDetailQuery` 保留(返回 `SkillTicketDetail`,现含 `messages`)。

(b) **删除**:`useAddSkillTicketComment`、`useUpdateSkillTicketStatus`、`useUploadSkillTicketAttachment`、`useSkillTicketAttachmentsQuery`、`downloadSkillTicketAttachment`、`attachKey`/`_attachKey`。

(c) **新增**(放在保留的 mutation 附近):

```ts
import type { SkillTicketMessage } from '@/api'

// invalidateAll:消息/重开等可能影响多视图,统一失效 detail + admin + mine + badge。
function invalidateAll(client: ReturnType<typeof useQueryClient>, id: string) {
  void client.invalidateQueries({ queryKey: detailKey(id) })
  void client.invalidateQueries({ queryKey: adminKey() })
  void client.invalidateQueries({ queryKey: mineKey() })
  void client.invalidateQueries({ queryKey: badgeKey() })
}

// useSendTicketMessage 发送文本消息(POST /skill-tickets/:id/messages)。
export function useSendTicketMessage(id: Ref<string | undefined>) {
  const client = useQueryClient()
  return useMutation({
    mutationFn: async (input: { text: string }) =>
      (await apiRequest<{ message: SkillTicketMessage }>(`/api/v1/skill-tickets/${id.value}/messages`, {
        method: 'POST', body: input,
      })).message,
    onSuccess: () => { if (id.value) invalidateAll(client, id.value) },
  })
}

// useUploadTicketMessage 上传图片/文件作为一条消息(multipart,POST /:id/messages/upload)。
export function useUploadTicketMessage(id: Ref<string | undefined>) {
  const client = useQueryClient()
  return useMutation({
    mutationFn: async (file: File) => {
      const form = new FormData()
      form.append('file', file)
      const resp = await xhrUpload(`/api/v1/skill-tickets/${id.value}/messages/upload`, { method: 'POST', body: form })
      return (resp.body as { message: SkillTicketMessage }).message
    },
    onSuccess: () => { if (id.value) invalidateAll(client, id.value) },
  })
}

// downloadTicketMessage 带鉴权下载文件消息(GET /:id/messages/:msgId/download)。
export async function downloadTicketMessage(ticketId: string, msg: SkillTicketMessage): Promise<void> {
  const { blob, filename } = await apiDownload(`/api/v1/skill-tickets/${ticketId}/messages/${msg.id}/download`)
  const url = URL.createObjectURL(blob)
  const a = document.createElement('a')
  a.href = url
  a.download = filename ?? msg.file_name ?? 'download'
  document.body.appendChild(a)
  a.click()
  a.remove()
  URL.revokeObjectURL(url)
}

// messageImageUrl 取图片消息的可内嵌预览 URL(走鉴权 apiDownload 转 objectURL)。
// 返回 objectURL,调用方在卸载时 revoke。
export async function fetchTicketMessageBlobUrl(ticketId: string, msg: SkillTicketMessage): Promise<string> {
  const { blob } = await apiDownload(`/api/v1/skill-tickets/${ticketId}/messages/${msg.id}/download`)
  return URL.createObjectURL(blob)
}

// useStartTicket 开始制作(POST /:id/start)。
export function useStartTicket() {
  const client = useQueryClient()
  return useMutation({
    mutationFn: async (input: { id: string }) =>
      apiRequest<void>(`/api/v1/skill-tickets/${input.id}/start`, { method: 'POST' }),
    onSuccess: (_d, v) => invalidateAdmin(client, v.id),
  })
}

// useReopenTicket 重新受理(POST /:id/reopen,rejected→processing)。
export function useReopenTicket() {
  const client = useQueryClient()
  return useMutation({
    mutationFn: async (input: { id: string }) =>
      apiRequest<void>(`/api/v1/skill-tickets/${input.id}/reopen`, { method: 'POST' }),
    onSuccess: (_d, v) => invalidateAdmin(client, v.id),
  })
}

// useUpdateTicketTargets 编辑已交付可见范围(PATCH /:id/targets)。成功后失效详情 + 市场缓存。
export function useUpdateTicketTargets() {
  const client = useQueryClient()
  return useMutation({
    mutationFn: async (input: { id: string; targets: DeliverTarget[] }) =>
      apiRequest<void>(`/api/v1/skill-tickets/${input.id}/targets`, { method: 'PATCH', body: { targets: input.targets } }),
    onSuccess: (_d, v) => {
      void client.invalidateQueries({ queryKey: detailKey(v.id) })
      void client.invalidateQueries({ queryKey: ['skills', 'market'] })
    },
  })
}
```

(d) `web/src/api/index.ts`:加 `export type SkillTicketMessage = WithRequired<Schemas['service.SkillTicketMessageResult'], 'id' | 'kind'>`;删 `SkillTicketComment`、`SkillTicketAttachment` 导出。

- [ ] **Step 4: 跑确认通过**

Run: `cd web && npx vitest run src/api/hooks/useSkillTickets.spec.ts && npx vue-tsc --noEmit`
Expected: PASS;**vue-tsc 会在引用旧 hook 的组件处报错——预期,后续 task 修复。**

- [ ] **Step 5: 提交**

```bash
git add web/src/api/hooks/useSkillTickets.ts web/src/api/hooks/useSkillTickets.spec.ts web/src/api/index.ts
git commit -m "feat(web): 工单 hooks 重写为消息/状态动作/可见范围

加 useSendTicketMessage/useUploadTicketMessage/downloadTicketMessage/useStartTicket/
useReopenTicket/useUpdateTicketTargets;删评论/附件/通用状态 hooks。"
```

---

## Task 2: TicketStatusStepper + TicketTargetsEditor

**Files:**
- Create: `web/src/components/ticket/TicketStatusStepper.vue`、`web/src/components/ticket/TicketTargetsEditor.vue`
- Test: 各自 `.spec.ts`

- [ ] **Step 1: 写失败测试**

`TicketStatusStepper.spec.ts`:

```ts
// 渲染三步(待处理/制作中/已交付)并高亮当前;rejected 状态显示「已拒绝」红色标记。
it('highlights current step', () => {})
it('shows rejected badge for rejected status', () => {})
```

`TicketTargetsEditor.spec.ts`:

```ts
// v-model 渲染每行 org+受众下拉;加组织追加一条;移除一条;受众改变 emit update。
it('renders rows and emits update on change/add/remove', () => {})
```

- [ ] **Step 2: 跑确认失败** → 组件不存在。

- [ ] **Step 3: 写实现**

`TicketStatusStepper.vue`:props `status: string`。三步主线 `pending→processing→delivered`,按当前状态高亮已达步骤;`rejected` 时在主线旁渲染红色「已拒绝」标记。用 Naive `n-steps`/`n-step` 或自绘 pill(参照 brainstorm mockup 的步骤条样式)。`defineProps<{ status: string }>()`。

`TicketTargetsEditor.vue`:`defineProps<{ modelValue: DeliverTarget[]; orgs: {id:string;name:string}[] }>()` + `defineEmits<{ 'update:modelValue': [DeliverTarget[]] }>()`。内部渲染每条 `{org 名 + audience n-select(整企业/仅管理员/仅申请人) + 移除}`,下方「加组织」下拉(排除已选)。改动时 emit 新数组。**逻辑直接抽自 `CustomSkillTicketsPage.vue` 现有 `targets`/`audienceOptions`/`addableOrgOptions`/`addTarget`/`removeTarget`/`orgLabel`**(把那段做成受控组件)。

- [ ] **Step 4: 跑确认通过** → `cd web && npx vitest run src/components/ticket/TicketStatusStepper.spec.ts src/components/ticket/TicketTargetsEditor.spec.ts`

- [ ] **Step 5: 提交**

```bash
git add web/src/components/ticket/TicketStatusStepper.* web/src/components/ticket/TicketTargetsEditor.*
git commit -m "feat(web): 工单状态步骤条与可见范围编辑器共享组件"
```

---

## Task 3: TicketConversation(统一消息流 + composer)

**Files:**
- Create: `web/src/components/ticket/TicketConversation.vue`
- Test: `web/src/components/ticket/TicketConversation.spec.ts`

- [ ] **Step 1: 写失败测试**

```ts
// text 消息按 author 是否当前用户分左右气泡,显示发送时间。
it('renders text bubbles aligned by author with timestamp', () => {})
// image 消息渲染缩略图(<img>),file 消息渲染文件名+下载按钮。
it('renders image thumbnail and file download', () => {})
// composer:输入文本点发送调用 send hook;选文件调用 upload hook。
it('sends text and uploads file via composer', () => {})
```

- [ ] **Step 2: 跑确认失败**。

- [ ] **Step 3: 写实现**

`TicketConversation.vue`:
- `defineProps<{ ticketId: string; messages: SkillTicketMessage[]; currentUserId: string | undefined }>()`。
- 内部:`const id = computed(() => props.ticketId)`;`useSendTicketMessage(id)`、`useUploadTicketMessage(id)`、`downloadTicketMessage`、`fetchTicketMessageBlobUrl`。
- 渲染消息列表:每条 `author_user_id === currentUserId ? mine : theirs`;显示作者占位 + `fmtTime(created_at)`;按 `kind`:
  - `text` → 气泡文本;
  - `image` → `<img>`(src 来自 `fetchTicketMessageBlobUrl` 异步解析的 objectURL,onMounted 解析、onUnmounted revoke;加载前占位),点击放大(简单用 `n-image` 或新窗);
  - `file` → 文件名 + 大小 + 下载按钮(`downloadTicketMessage`)。
- composer:`n-input` textarea + 「发送」按钮(send text);隐藏 `<input type=file>` + 「图片/文件」按钮(upload)。发送后清空。
- 图片 objectURL 用一个 `Map<msgId,string>` 缓存并在卸载时统一 revoke,避免泄漏。

- [ ] **Step 4: 跑确认通过**。

- [ ] **Step 5: 提交**

```bash
git add web/src/components/ticket/TicketConversation.*
git commit -m "feat(web): 工单统一消息对话组件(text/image/file + composer)"
```

---

## Task 4: DeliverCustomSkillModal(抽离交付弹窗)

**Files:**
- Create: `web/src/components/ticket/DeliverCustomSkillModal.vue`
- Test: `web/src/components/ticket/DeliverCustomSkillModal.spec.ts`

- [ ] **Step 1: 写失败测试**

```ts
// 粘贴 Markdown 解析出 name 预览;确认交付调用 useDeliverCustomSkill,目标范围用 TicketTargetsEditor 的值。
it('parses markdown and delivers with targets', () => {})
```

- [ ] **Step 2: 跑确认失败**。

- [ ] **Step 3: 写实现**

把 `CustomSkillTicketsPage.vue` 现有交付弹窗整段(`deliverOpen`/mode/mdText/folder/parsed/`onDeliver`/skillPackaging 调用)抽成组件:
- `defineProps<{ show: boolean; ticket: SkillTicketDetail; orgs }>()` + `defineEmits<{ 'update:show':[boolean]; delivered:[] }>()`。
- 内部用 `TicketTargetsEditor` 承载 targets(默认据 `ticket.requester_role` 推导,沿用现 `defaultTargets`)。
- `useDeliverCustomSkill`;成功 emit `delivered` + 关闭。409 提示「迭代必须沿用同一技能名」(沿用现逻辑)。

- [ ] **Step 4: 跑确认通过**。

- [ ] **Step 5: 提交**

```bash
git add web/src/components/ticket/DeliverCustomSkillModal.*
git commit -m "feat(web): 交付弹窗抽离为共享组件(复用打包逻辑+可见范围编辑器)"
```

---

## Task 5: TicketDetailPage(role-aware 整页)+ 路由 + keep-alive

**Files:**
- Create: `web/src/pages/skill-tickets/TicketDetailPage.vue`
- Test: `web/src/pages/skill-tickets/TicketDetailPage.spec.ts`
- Modify: `web/src/app/router.ts`、`web/src/layouts/DashboardLayout.vue`

- [ ] **Step 1: 写失败测试**

```ts
// 平台管理员 + pending:显示「开始制作」「拒绝」;processing:显示「交付」「拒绝」;
// delivered:显示「迭代交付」「编辑可见范围」,无状态手改;rejected:显示「重新受理」。
it('renders admin actions per status', () => {})
// 需求方(org_member):无管理员动作;delivered 显示「去安装」;能看到对话与状态步骤条。
it('renders requester read-only with go-install on delivered', () => {})
// 报价:管理员 pending/processing 可编辑,delivered 只读;需求方只读。
it('quote editable only for admin in pending/processing', () => {})
```

- [ ] **Step 2: 跑确认失败**。

- [ ] **Step 3: 写实现**

`TicketDetailPage.vue`:
- 取 `route.params.id`;`const id = computed(() => String(route.params.id))`;`useSkillTicketDetailQuery(idRef)`。
- `auth.isPlatformAdmin` 决定 admin 视图。
- 顶部 header:返回链接 + 标题 + `TicketStatusStepper :status` + 右侧动作区:
  - admin:按 `detail.status` 条件渲染按钮 —— pending:`开始制作`(useStartTicket)+`拒绝`;processing:`交付`(开 DeliverCustomSkillModal)+`拒绝`;delivered:`迭代交付`(DeliverCustomSkillModal)+`编辑可见范围`(开 TicketTargetsEditor 弹窗 + useUpdateTicketTargets);rejected:`重新受理`(useReopenTicket)。`拒绝` 开 reject 弹窗(useRejectSkillTicket,抽现有 reject modal)。
  - requester:delivered 时 `去安装`(`router.push('/skills')` 或沿用 SkillManager 联动;最简:跳 `/skills` 让用户在市场定制筛选安装)。
- 需求块:`detail.description`。
- 可见范围块:展示当前 targets(delivered 时 admin 显示「编辑可见范围」入口)。
- 报价块:admin 且 status∈{pending,processing} → `n-input-number` + 保存(useSetSkillTicketQuote);否则只读展示。
- `TicketConversation :ticket-id="id" :messages="detail.messages" :current-user-id="auth.user?.id"`。

`router.ts`:加路由(无 allowedRoles,页面内按角色渲染;后端已做权限):

```ts
{ path: 'skill-tickets/:id', component: () => import('@/pages/skill-tickets/TicketDetailPage.vue') },
```

`DashboardLayout.vue`:把 `<RouterView />`(第 102 行)改为带 keep-alive(保活两个列表页):

```vue
<RouterView v-slot="{ Component }">
  <keep-alive :include="['CustomSkillTicketsPage', 'OrgSkillsPage']">
    <component :is="Component" />
  </keep-alive>
</RouterView>
```

(两个被保活组件需在 script setup 里 `defineOptions({ name: 'CustomSkillTicketsPage' })` / `name: 'OrgSkillsPage'`,见 Task 6 + OrgSkillsPage 修改。)

- [ ] **Step 4: 跑确认通过**。

- [ ] **Step 5: 提交**

```bash
git add web/src/pages/skill-tickets/ web/src/app/router.ts web/src/layouts/DashboardLayout.vue
git commit -m "feat(web): 工单共享整页详情(role-aware)+ 路由 + 列表 keep-alive"
```

---

## Task 6: 平台队列 + 需求方面板瘦身为「列表→整页」

**Files:**
- Modify: `web/src/pages/platform/CustomSkillTicketsPage.vue`、`web/src/components/SkillTicketPanel.vue`、`web/src/pages/skills/OrgSkillsPage.vue`
- Test: 更新两者既有 spec

- [ ] **Step 1: 改测试**

- `CustomSkillTicketsPage.spec.ts`:断言「处理」按钮点击 `router.push('/skill-tickets/<id>')`;移除抽屉/交付/拒绝相关断言。
- `SkillTicketPanel.spec.ts`:断言「查看」点击 `router.push('/skill-tickets/<id>')`;保留列表/提交断言;删抽屉/附件/评论断言。

- [ ] **Step 2: 跑确认失败**。

- [ ] **Step 3: 改实现**

- `CustomSkillTicketsPage.vue`:`defineOptions({ name: 'CustomSkillTicketsPage' })`;**删除**抽屉、交付弹窗、拒绝弹窗、状态/报价操作区及相关 script(全部迁入 TicketDetailPage/组件);保留队列查询+筛选+表格;表格「处理」`onClick` 改 `router.push('/skill-tickets/' + row.id)`。文件大幅变小。
- `SkillTicketPanel.vue`:`defineOptions` 不需要(它在 OrgSkillsPage 内);删除详情抽屉、附件、评论、回复相关模板与 script;保留列表 + 提交弹窗;「查看」改 `router.push('/skill-tickets/' + row.id)`;「去安装」保留(delivered 行)。**提交弹窗增强**:允许可选附文件;`onSubmit` 改为:先 `submitMut`(创建工单)拿到 `ticket.id`,再对所选文件逐个 `useUploadTicketMessage` 发送(落为最早的文件消息),最后跳详情或刷新列表。
- `OrgSkillsPage.vue`:`defineOptions({ name: 'OrgSkillsPage' })`(供 keep-alive include)。

- [ ] **Step 4: 跑确认通过 + 全量**

Run: `cd web && npx vitest run && npx vue-tsc --noEmit && npm run build`
Expected: 全绿、类型干净、build 成功。

- [ ] **Step 5: 提交**

```bash
git add web/src/pages/platform/CustomSkillTicketsPage.vue web/src/components/SkillTicketPanel.vue web/src/pages/skills/OrgSkillsPage.vue web/src/pages/platform/*.spec.ts web/src/components/*.spec.ts
git commit -m "feat(web): 平台队列与需求方面板瘦身为列表→整页

移除内嵌抽屉/交付/拒绝(迁入整页);行操作跳 /skill-tickets/:id;提交可附文件落消息;
两列表页 defineOptions name 供 keep-alive。"
```

---

## Task 7: 全量校验 + 三角色浏览器端到端

**Files:** 无(校验)

- [ ] **Step 1: 部署本地 k3d**

Run: `make local-build`(前端镜像重建 + rollout;Plan 1 迁移已应用)。

- [ ] **Step 2: 三角色浏览器验证(真实浏览器,逐项留证)**

- **平台管理员**:队列「处理」→整页;pending 点「开始制作」→步骤条进制作中;改报价;「交付」(粘贴 MD)→自动已交付、步骤条到已交付;「编辑可见范围」改受众→保存→市场可见性随变;对已拒绝工单「重新受理」→制作中;`keep-alive`:返回队列保住滚动/筛选,且刚处理那行状态已更新;delivered 工单不再能手改状态。
- **企业员工**:提交需求(附一张图片+一个文件)→列表;「查看」→整页只读状态步骤条;发文字消息、发图片(内嵌预览)、发文件(下载);在已交付/已拒绝工单发消息→自动重开(步骤条回待处理);delivered「去安装」。
- **企业管理员**:同员工路径验证一遍(只读+发消息+去安装);差异化可见性(仅管理员可见的定制技能)经管理员侧整页验证。
- **对话细节**:图片缩略图、文件下载、时间戳、左右气泡归属正确;附件即消息。

- [ ] **Step 3: 交付前检查**

工作区无无关改动;`make openapi-check` 干净(Plan 1 已同步);逐项留证;发现问题先修再验。

---

## Self-Review(计划自查)

- **Spec 覆盖**:整页 role-aware(Task 5)、状态步骤条/对话/可见范围编辑器/交付弹窗四组件(Task 2/3/4)、hooks 消息+动作+targets(Task 1)、keep-alive 双列表(Task 5/6)、队列与面板瘦身+提交附件落消息(Task 6)、三角色验证(Task 7)。覆盖齐。
- **占位符扫描**:组件实现指明「抽自现有某段」并给出受控 props/emits 契约,非含糊;图片预览 objectURL 生命周期已说明。无 TBD。
- **类型一致**:`SkillTicketMessage`(Task 1 定义)贯穿 Task 3/5;`DeliverTarget` 复用;hooks 名(useSendTicketMessage/useUploadTicketMessage/useStartTicket/useReopenTicket/useUpdateTicketTargets/downloadTicketMessage/fetchTicketMessageBlobUrl)Task 1 定义、Task 3/5/6 调用一致;keep-alive include 名与 `defineOptions({name})`(Task 5 要求、Task 6 落地)一致。
